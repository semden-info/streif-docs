# Streif — Decisions Log (Журнал рішень / ADR-статуси)

> **Призначення:** єдине канонічне джерело статусу рішень. Кожне рішення має статус: **accepted** (ухвалено), **proposed** (відкрите), **superseded** (замінене). Деталі — у відповідних `01–10`. Створено після Фази-0 gate (стороннє review + адверсивний тех-стек-пас), 2026-06-21.

---

## Ухвалені (accepted)

| # | Рішення | Деталі |
|---|---|---|
| D1 | **Механіка** — зафарбовування на **повністю видимій** карті (НЕ fog-of-war) | `01` §4 |
| D2 | **Реframe** — «персональна мапа новизни», що спонукає до detour; будинки = перша візуальна одиниця відкриття, не кінцева сутність. **Центральна метрика успіху — чи людина справді обрала новий маршрут** | `01` §1/§9 |
| D3 | **MVP-шар — місто-first** | `09` §1 |
| D4 | **MVP — device-local**, два кроки MVP-0 / MVP-1 | `09` §2 |
| D5 | **Матчинг — радіус (MVP), уточнений:** буфер **сегмента** треку (не точки) · враховувати GPS-accuracy · мінімальне реальне переміщення · Activity Recognition як **підказка**, не єдиний gate · vehicle-фільтр із **гістерезисом** (без жорсткого порогу 3–4 м/с). Street-corridor → пізніше | `05` §5 |
| D6 | **Eligibility (B)** — мова «**розкрито/побачено**» (не «відвідано») · без «100%»-completionism · дешевий **build-time фільтр** (будинок рахується лише якщо поруч публічна піша мережа) · повний frontage → fast-follow. **Лок (2026-07-04):** джерело пішої мережі = **OSM `highway`** (footway/pedestrian/residential/living_street/service/path/steps; turrutebasen — стежки природного шару, для міста не те; Elveg — запасний, слабший з тротуарами); **буфер ~25–30 м** (будинок eligible якщо контур ≤буфера від пішохідної дороги; велике шосе motorway/trunk саме по собі НЕ зараховує як пішохідну); прапорець `eligible` рахувати **на сервері** в CC-BY-пайплайні (не на телефоні). Тонке «мій бік vs через дорогу» → **fast-follow** після MVP-0. Буфер польово-тюнабельний (як R_FAR). ⚠️ Ліцензія: OSM=ODbL → використати лише для build-time розрахунку булевого `eligible` (не публікуючи OSM-геометрію); підтвердити «produced work» / розглянути Elveg при міграції (P4). **Реалізовано (dogfood-клієнт) 2026-07-04:** `AreaSource` тягне highway → прапорець `accessible` (buffer 28м, grid), гейт у `matchAt`. Verified (field+пристрій): **112/112 should-розкриттів збережено (нуль хибних негативів)**, відсіяно ~8% недосяжних; ⚠️ across-road НЕ рятує (5/5 should-not лишились accessible → «мій бік vs протилежний» = fast-follow). **Проста side-aware спроба (crossing-check GPS→центр) 2026-07-04 ВІДКОЧЕНА** — ламала 45% правильних розкриттів у щільному місті (residential-дороги всюди) і не ловила target (хибні reveals = мікс across-residential/«зарано», не чиста геометрія). Потрібен «той самий бік/тротуар», не сирий crossing, + більше міток | `09` §3, P4 |
| D7 | **Рендер — двошаровий** (сіра PMTiles-база + visited GeoJSON-overlay), гартований: `withSynchronousUpdate(false)`/**async** (sync ≈2× латентність + колапс fps при пан+апдейті — спростовано на spike, D22) · overlay тільки visited + viewport-scoped · стабільний `building_id` · pin свіжого стабільного MapLibre · **native MapView у Compose** (не wrapper). Feature-state Android — **немає в 13.x** (#1698 open, PR #4219 unmerged) → двошаровий єдиний (D22) | `05` §3.1 |
| D8 | **⚠️ УТОЧНЕНО D31 (2026-07-04): INSPIRE не покриває наш регіон (Sunnmøre, verified) → продакшн-геометрія = OSM self-host (ODbL) + Matrikkelen-збагачення; INSPIRE/FKB — коли з'явиться покриття/публічний партнер.** Первісний план (superseded D31): геометрія з INSPIRE Core2d + тип з Matrikkelen, ship CC-BY. **Чинне (D31): геометрія OSM (ODbL) + тип/номер Matrikkelen (CC-BY) → комбінований ODbL-тайл**; INSPIRE/FKB — апгрейд потім (покриття/партнер, P4). **Spike (наживо 2026-06-21):** INSPIRE WFS зараз віддає лише **точки, не полігони** (підтверджує README `building2osm`) → spike-дані з **OSM** (Volda, 10.8k, `spike/buildings.pmtiles`); чистий Geonorge-CC-BY — продакшн-задача | `06` §3, `11` |
| D9 | **База — Kartverket WMTS `topograatone`** (raster); офлайн через **pre-cache регіону** (OfflineManager) або bundle; ізолювати за інтерфейсом; vector — пізніше | `05` §3.2 |
| D10 | **Tiles — PMTiles**: bundled у assets → копія в `filesDir` → `pmtiles://file://` (бо `asset://` крешить, див. D21); CDN пізніше | `05` §7 |
| D11 | **Сховище — Room + BundledSQLiteDriver**. **Реалізовано 2026-07-04 (MVP-0-старт):** `MvpDatabase` (visits+sessions, колонка `source` за D30), KSP `2.2.10-2.0.2`+Room `2.8.4`, importer `visited.txt`→Room (132 dogfood-розкриття мігрували, verified на пристрої), session-checkpoint (лікує D23 #11), DEBUG-дубль у `visited.txt` для `analyze.py`. **⚠️ AGP 9.2.1 built-in Kotlin потребує `android.disallowKotlinSourceSets=false` для KSP.** Spike-шорткати→полір: класичний драйвер (BundledSQLiteDriver пізніше) + `allowMainThreadQueries` (винести на IO-dispatcher) | `05` §3, CHANGELOG |
| D12 | **Backend — НЕ в MVP.** Build-time пайплайн = CLI (`building2osm`/GDAL → Tippecanoe → PMTiles), без БД. Laravel/Filament/PostGIS/Sanctum/Redis/FCM — лише v2, обирати після sync/social-вимог | `05` §8 |
| D13 | **Метрики — три виміри: Coverage / Variety / Discovery.** Без єдиного score/XP, без формул балів. «Discovery» у MVP = особиста новизна; rarity — лише **описова мітка**, ніколи не множник (стелю ×2 знято 2026-07-31, **D41**). **⚠️ Уточнення (2026-07-11, D33):** керує **дефолтним/спокійним** досвідом; opt-in шар гейміфікації (post-MVP, D33) може нести багатші механіки, але rarity/difficulty **НІКОЛИ не множник нагороди** (Merkehåndboka — лише описова/safety-мітка), а «стелі рідкісності» більше не існує — ×2 знято 2026-07-31 (**D41**), бо сам ×2 і був множником; рідкість виражає структура наборів; безпека тримається крос-режимно | `04`, D33 |
| D14 | **Приватність — device-local мінімізація; сирий GPS ніколи на сервер.** Diagnostic mode тримає сирий трек **локально ~30 днів** (ручне видалення; у релізі — налаштування) | `07` §4/§6 |
| D15 | **Offline-first (уточнено)** — tiles будинків bundled; база pre-cached/bundled для регіону; **не** live WMTS у полі | `01` §7, `05` §3.2 |
| D16 | **Регіон MVP — Volda + Ørsta**; перший зріз spike — **Volda** (kommune 1577) | `09` §7, `11` |
| D17 | **Spike — розділений:** Spike 1 (реальні дані → PMTiles → **симульований GPS** → кольорування), Spike 2 (реальний FGS + diagnostic) | `10` §4 |
| D18 | **Локалізація** — bokmål + nynorsk + англ.; default за пристроєм (fallback NO→EN). **Каркас реалізовано 2026-07-04:** `values/`(en)+`values-nb`+`values-nn`, UI+нотифікація; норв. переклади чернеткові (звірити носієм); діагностичні нотатки gate/loading — follow-up | `08` §11, CHANGELOG |
| D19 | **Фреймворк** — native Kotlin + Compose; логіку тримати в чистих модулях для дешевого KMP-iOS пізніше | `05` §3 |
| D20 | **Дух гейміфікації** — спокій, без streak, святкувати віхи без тиску, тижневий підсумок (in-app, без push) | `04` |
| D21 | **Spike-1 (рендер) ✅ підтверджено на Pixel 9 (2026-06-21):** MapLibre Native рендерить ~10.8k кольорових будинків Volda. Знахідки: (а) **PMTiles вантажити `pmtiles://file://`** (копія asset→filesDir на 1-му запуску); **`pmtiles://asset://` крешить** `incorrect header check` (баг AssetFileSource, усі версії 12.3.1–13.3.0); (б) `.pmtiles`-asset потребує **`noCompress`**; (в) тайли — **tippecanoe** (не GDAL); (г) рендер так само ок із GeoJSON-джерелом (запасний шлях) | `05` §3.1, `11` |
| D22 | **Spike-1 v2 (механіка+перф) ✅ provisional на Pixel 9 (2026-06-22):** двошаровий overlay (сіра PMTiles-база + visited `GeoJsonSource`, колір за типом) тримає **~60 fps пану до 4000 visited** у viewport; **async** `setGeoJson`-латентність ≤~100 мс до 2000 (148 мс@4000); пам'ять стабільна (GC-sawtooth, без витоку, ~41 МБ@1387). Радіус-grid матчинг точний (1387/1387 по маршруту). **sync — погано:** ≈2× латентність + **колапс fps при пан+апдейті** (N=1000/2000/4000 → 48/19/5 fps проти стабільних ~60 async). Інструмент — **власний MapLibre frame-listener** (HWUI-tools SurfaceView не бачать). Gate **відкритий до mid-range** (Pixel 9 — флагман) | `05` §3.1, `11` §2–5 |
| D23 | **Незалежне review — наступне на межі Spike 2 → MVP-0** (повний стан проєкту + tech-stack-optimality re-check; spike→продукт = остання дешева мить виправити фундамент). Далі вужче: **data/licensing** при CC-BY-пайплайні (P4), **повне+security** перед v2 (backend/social). Формат: адверсивний review свіжими агентами (без вкладення в попередні рішення) + опц. людина-рецензент | `10`, P4/P5 |
| D23.1 | **Review ВИКОНАНО (2026-07-01) — вердикт «готово до MVP-0 з умовами».** Дві незалежні гілки (10-агентний адверсивний воркфлоу Opus: 18 знахідок→16 підтв./2 спрост.; + codex gpt-5.4), знахідки верифіковано по коду. **Архітектура здорова — переробки не треба** (MapLibre Native, двошаровий overlay, `AreaSource`-шов, flat-файли-на-тюнінг). **3 реліз-блокери:** (1) `allowBackup=true`→`filesDir` (у debug — сирий diag.csv) у Google-хмару = порушення D14; (2) Overpass ODbL→CC-BY CDN; (3) `building_id` не переживе зміну джерела (D30). **Кластер matching/cold-start**, що спотворює польові дані: тангенційний промах (корінь Ristevegen 1), буфер replay 12≈24с < часу fetch, `ensureArea` під dwell-гейтом. **Застосовано 3 дешеві фікси** (allowBackup=false; ensureArea з-під gate; reattach store). Повний список і перебіг → CHANGELOG 2026-07-01 | CHANGELOG, D30/P2 |
| D24 | **On-demand area download** (заміна фіксованого бандлу — польовий тест 2026-06-26 довів, що bbox не масштабуються під рух користувача: розкривалось лише там, де вгадано зону). `BuildingStore` інкрементальний + `AreaLoader` тягне будинки в bbox навколо GPS на льоту + локальний кеш; **джерело за інтерфейсом**. **Тест = Runtime Overpass** (ODbL, нуль інфри, флакі — ок для dogfood, зону тягнемо раз і кешуємо). **⚠️ ОБОВ'ЯЗКОВО мігрувати на Pre-hosted CC-BY CDN ПЕРЕД публічним релізом** (надійність + масштаб для багатьох юзерів + ліцензія) — конкретний тригер для **P4 / D8 / D10**. Запит Дениса 2026-06-26: не забути цей перехід | `05` §7, P4 |
| D25 | **Матчинг — edge-distance + closest-approach** (заміна центроїд-within-R; польовий присуд 2026-06-26: 2147 фіксів + 91 ручна мітка + adversarial-воркфлоу). Корінь ОБОХ скарг («зарано» + «пропуск через дорогу») — міряли відстань до **центроїда**, не до **контуру**. Зміни: (1) GPS→**контур полігона** (point-to-ring, PIP→0), локальна проєкція за широтою запиту; (2) **bbox-grid** — будинок у всі комірки, що накриває його bbox (великий/видовжений із далеким центроїдом не випадає); (3) **R_FAR=18 м**; (4) **closest-approach gate** — розкрити коли edge виросла на **SLACK=1.5 м** над мінімумом (проминув найближчу точку) АБО стіна ≤ **MIN_EDGE=8 м** (впритул-байпас). **closest-approach, НЕ abreast:** adversarial-рев'ю показало курс на 2-м базі шумний (P(похибка>75°)=36%) → abreast крихкий; closest-approach course-free, помиляється «пізніше». **Replay на тому ж треку:** recall 69→76/83, edge@reveal 19.2→9.9 м, видовжені «пройшов за 2-6 м» тепер ловляться. `R_FAR`/`MIN_EDGE`/`SLACK` легко-конфігуровані для польового тюнінгу (P2). НЕ чіпати: vehicle-gate, ACC_MAX=30, building_id | `05` §5, P2 |
| D25.1 | **Тайминг матчингу — closest-approach до ЦЕНТРОЇДА** (рефайн D25, польовий ретест 2026-06-27: 1563 фікси + 66 міток). Скарга «розкривається до того, як дійшов» лишалась, бо `MIN_EDGE=8` спрацьовував на **передньому куті** будинку. Денис: ідеал — розкривати «**приблизно по середині, коли йдеш уздовж**». Зміни: **eligibility** лишається edge ≤ R_FAR=18 (через дорогу/видовжені), а **тайминг** тепер по closest-approach до **ЦЕНТРОЇДА** (`runMin` центроїд-дист; розкрити коли виросла на **SLACK_CEN=2 м** над мінімумом = проминув середину) + малий байпас **MIN_EDGE_NEAR=3 м**/всередині (впритул) замість MIN_EDGE=8 + **`moved`-guard** (тайминг-розкриття лише на реальному русі ≥MIN_MOVE — проти GPS-джитера на місці). **Replay на тому ж треку:** розкриття лягає медіана **3.3 м від центру** (79% у межах 5 м) проти переднього кута; ціна — recall 61→56/61 (будинки, повз середину яких не пройшов). `SLACK_CEN`/`MIN_EDGE_NEAR` — тунабельні (P2) | `05` §5, P2 |
| D26 | **Deferred auth — пропонувати, не вимагати** (перенесено з P9, 2026-06-27) — логін не на старті; пропонувати акаунт/збереження прогресу **після** розкриття частини карти (момент цінності). MVP-0 device-local без логіну (D4); формалізує onboarding + шлях до опц. cloud-backup (v2) | `08` §10, D4/D14 |
| D27 | **POI «цікаві місця» + фото/нотатки — у природному шарі** (перенесено з P11, 2026-06-27) — (1) read-only курований POI з відкритих даних (Stadnamn+lore, turrutebasen, DNT, Wikidata) без backend; (2) особисті локальні нотатки/фото device-local (D14); (3) публічні фото/відгуки — лише v2 (backend+модерація+DPIA). Увесь блок — природний шар, НЕ міська версія | `06`, `07`, D12/D14 |
| D28 | **Візуальна мова — мінімалізм, без перевантаження** (перенесено з P12, 2026-06-27) — прості контурні іконки колір-за-типом (приклад Walksy), низька щільність маркерів (анти-StreetComplete), ≥48dp + читабельні написи, дорослий тон. Collect/Unlock = приємне «розкрито» + колекція, **без абстрактного score** (лічильники повноти — ок; м'яка гейміфікація — P13) | `08` §1/§8/§14, D13/D20 |
| D29 | **UX точності GPS — не розкривати/не пропускати тихо** (перенесено з P14, 2026-06-27) — показувати стан надійності GPS, не мовчазний мис-reveal/пропуск (урок Turf); доповнює edge-матчинг | `05` §5, D25/P2 |
| D30 | **Захист від ретроактивної зміни «розкрито»** (перенесено з P15, 2026-06-27) — апдейти даних Kartverket не «знімають» уже розкрите (анти CityStrides/OSM); стабільний `building_id` (D7/D22) + інваріант «розкрите лишається розкритим». **⚠️ D23-review (2026-07-01): поточний `building_id="w"+wayId` (OSM) НЕ переживе Overpass→CC-BY (INSPIRE/Matrikkelen — інший id-простір) → усі персистовані розкриття осиротіють, мапа тихо скинеться. Найвищий незакритий ризик міграції. **УХВАЛЕНО (2026-07-04):** продакшн `building_id` = Matrikkelen `bygningsnummer` (напр. `m<nr>`), приходить у CC-BY-пайплайні (join INSPIRE-геометрія + Matrikkelen тип+номер). Overpass-era dogfood-розкриття = одноразові (clear при зміні джерела) АБО опц. одноразовий centroid→будинок spatial-remap історії Дениса; Room несе колонку `source` (osm-test/matrikkelen). Геометричний хеш (centroid-geohash) **відхилено** — OSM≠Matrikkelen-контур дає зсув + колізії сусідніх дрібних будівель. Додати SOURCE_VERSION у ключ кешу (інакше старі Overpass-тайли завантажаться як CC-BY).** | `05`, D7/P4, D23.1 |
| D31 | **CC-BY продакшн-дані — рішення пайплайну (лок 2026-07-04; конкретизує D8/D24/D6/D30/P4).** (1) **Eligibility-джерело продакшн (server-side D6) = Kartverket Elveg** (дороги; чиста CC-BY-сумісна ліцензія — на відміну від OSM ODbL; OSM-highway лишається ЛИШЕ dogfood-клієнт). (2) **Хостинг = безкоштовний статичний CDN** (Cloudflare Pages/R2 або аналог; некомерційний соло-проєкт). (3) **Обсяг = регіон-first** (Volda/Ørsta → Møre og Romsdal), пайплайн масштабований на всю Норвегію. (4) **Міграція Overpass-era розкриттів = одноразово стерти** (dogfood-тестові; spatial-remap лише якщо зберігати історію). Формат = per-tile GeoJSON на сітці 0.02°; id = Matrikkelen bygningsnummer (D30). **Крок 0 (data-спайк, ВИКОНАНО 2026-07-04):** прямі Geonorge WFS/API-тести → **INSPIRE Buildings НЕ покриває наш регіон** (Осло-bbox 499 буд., Møre og Romsdal-bbox **0**); точних відкритих CC-BY footprints для Sunnmøre нема (FKB `AccessIsRestricted:true`). **ОБРАНО: Варіант 1 — OSM-геометрія (self-host, ODbL) + Matrikkelen-збагачення** (bygningsnummer+тип через point-in-polygon join; CC-BY). **FKB = апгрейд ПОТІМ через публічного партнера** (Volda/Ørsta kommune, Møre og Romsdal fylkeskommune, Høgskulen i Volda — усі Norge-digitalt-партнери з доступом до FKB; дають дані+легітимність+спонсорство; приватному ENK FKB — платна ліцензія через Geodata/Norkart). **УТОЧНЕНО (2026-07-04, юр-research, першоджерела):** партнер НЕ може просто «дати» FKB для власного застосунку Дениса — субпідряд-договір Norge digitalt (`Standardavtale for utlån … underleverandører`) забороняє (no rights acquired / delete-after-assignment / non-commercial / лише завдання партнера); освітня bruksright HVO не покриває ліцензування FKB у публічний застосунок. Життєздатно ЛИШЕ якщо Streif = проєкт партнера (партнер = data controller, Денис = databehandler під його bruksrett) АБО партнер спонсорує купівлю. **OSM+Matrikkelen лишається основним шляхом — FKB не блокер.** Відкрите юр-питання (постійна utadrettet-послуга vs oppdrag-з-видаленням) → `post@norgedigitalt.no`. Стратегія+готовий лист → `docs/partner-outreach.md`. Комбінований тайл = ODbL (share-alike); атрибуція © OpenStreetMap + © Kartverket. Апгрейд без зміни архітектури (AreaSource-шов, Matrikkelen-номер як id). **РЕАЛІЗОВАНО (2026-07-04):** пайплайн `spike/pipeline/` (Matrikkelen GML + OSM Overpass → point-in-polygon join → D6-accessible → gzip-тайли 0.02°, id=`m<bygningsnummer>`); join Volda 98.5% / Ørsta 84% (стеля — OSM має більше об'єктів ніж Matrikkelen); **Volda+Ørsta = 416 тайлів (8.2→2.2 MB gzip) живі на Cloudflare R2** (`pub-b1c9ae365792405880b62e24ccda0df1.r2.dev`; r2.dev-now → custom-domain `tiles.streif.no` на першого зовнішнього тестера). **Хостинг обрано R2, не Pages** (Pages — стеля 20k файлів, впремося на нац.масштабі); **$0** (лімітує читання 10M/міс — юзаємо ~0.09%; egress безкоштовний; сховище 10 GB); **R2 сам НЕ стискає → тайли передстиснено** (Content-Encoding: gzip, ~57 KB найбільший). Клієнт: `CdnGeoJsonAreaSource` + `BuildConfig.USE_CDN`/`CDN_BASE_URL` (Android-код витримує 3 gzip/404/Content-Length-пастки). Хостинг-план верифіковано адверсивним воркфлоу (R2 vs Pages + 3 скептики: cost/prod/correctness, веб-джерела Cloudflare docs). **Elveg-eligibility ✅ ЗРОБЛЕНО (2026-07-04, Варіант B):** тайли-`accessible` рахуються з **Kartverket NVDB Vegnett Pluss** (= розповсюджуваний Elveg 2.0; UUID `97e6a869-…`, CC BY 4.0, GML, EPSG:5973≈25833; walkable-фільтр по `typeVeg` + `vegkategori`) **+ OSM-footpath-bridge** (A/B-гейт на Volda: Elveg-only регресував 309 буд. — hytte/outbuilding на неформальних `sti`/`traktorveg`, чия міграція в Elveg триває 2026–2027; bridge = нуль регресії, accessible регіону 83→87%). Обрано B бо тайл **і так ODbL** (геометрія OSM) → bridge не додає ліцензійної ціни зараз; прибрати коли геометрія→FKB І Elveg домігрує стежки, або +Turrutebasen. **⚠️ Pipeline-vs-runtime:** offline-CDN-тайли несуть Elveg-eligibility; runtime on-demand (Overpass, USE_CDN=false) на девайсі й далі рахує accessible з OSM-highways — вирівняється коли Elveg-мережа поїде pre-hosted. `parse_elveg` + `--elveg`/`--osm-bridge` у `spike/pipeline/build_tiles.py`; A/B → `elveg_ab.py`. **TODO:** custom-domain перед зовнішнім тестером + вимкнути r2.dev | `06`, D8/D24/D6/D30/P4 |
| D32 | **UI-стек = Jetpack Compose + base Material 3 (лок 2026-07-05; верифіковано адверсивним воркфлоу — 3 research + devil's-advocate + вердикт, веб-джерела 2026).** Розглянуто ВСІ 6 сучасних опцій: Compose+M3 ✅, Material 3 Expressive (à-la-carte), Flutter ❌, React Native ❌, KMP+Compose Multiplatform (не зараз), legacy Views ❌. Адверсивний агент чесно не зміг побудувати реальний кейс проти. **Обрано Compose + base M3** бо: нативний Kotlin (нуль-bridge до FGS/Fused/sensors/battery/Room — вони живуть НИЖЧЕ UI, framework-invariant); MapLibre Native Android v13.1.0 (first-party — обгортки Flutter/RN відстають на версії + Flutter має проблеми з фоновим GPS, ~$500/рік плагін); solo-dev (один skillset); View/XML заморожено (maintenance mode). **Уточнення до плану:** (1) писати Compose **«CMP-clean»** (UI-логіка поза Android-специфічними типами) → двері iOS через KMP+CMP відкриті БЕЗКОШТОВНО, портування замість rewrite; (2) **Material 3 Expressive à-la-carte** — base M3 `1.4.0` як floor, брати вже-graduated Expressive-компоненти з `1.5.0-alpha` вибірково (не чекати stable 1.5.0); (3) **map у Compose через `AndroidView` над наявним MapView** (перевірено), НЕ через pre-1.0 `maplibre-compose` v0.13.0. Візуальна мова = **D28 / `08`**. Відомий framework-invariant ризик: нативний OOM MapLibre при пануванні динамічних polygon-шарів (у SDK, який уже поставляємо) — відстежувати. **Реалізовано (2026-07-05, проміжно на Views до міграції):** emoji-grey-кнопки → Material `FloatingActionButton` (mini) + `MaterialButton` (pill) + векторні Material-іконки; повна Compose-міграція UI = окрема MVP-0-задача (план у `10`) | `05`, `08`, D28 |
| D33 | **Гейміфікація = спокійний кор + опційний шар, що вмикається** (лок 2026-07-11; обговорення Дениса + codex-review, звірено з D13/D20/D2). Замість вибору «спокій vs гейміфікація» — **спокійний кор (дефолт, MVP-1) + opt-in шар гейміфікації (post-MVP)**, «регулятор глибини», не форк; предметне втілення «спокій за замовчуванням, челенджі опційні» (`01` §7). **D13/D20 керують дефолтом.** Спірні механіки брейнсторму (`04-2` → влито в `04`) живуть в opt-in шарі під **крос-режимними інваріантами:** безпека **не буває opt-in** (жодна нагорода не штовхає в ризик); без втрати прогресу при перемиканні (D30); без streak/FOMO навіть у гейміфікованому; спокійний режим = **повний**, не тизер. Реалізація — дешевше через **гранульовані тумблери** (цілі/колекції/челенджі/серпанок), іменований пресет пізніше за попитом. Механіки → P21–P25; MVP-1 везе лише кор | `04`, D13/D20/D30 |
| D34 | **Розкривання об'єктів = 2 рівноправні стовпи (Місто / Природа), механіка за ГЕОМЕТРІЄЮ** (design-сесія Дениса 2026-07-12, метод point-by-point walkthrough; розширює **D27**, зчеплено з P6/P10/P16; Stikk UT — філософський прецедент «усі turmål рівні, без вагового score» = наша D13). Кор Streif лишається **ambient-reveal**; поверх — рівноправна «збирай»-механіка (turmål/POI з ВІДКРИТИХ даних). **8 ухвал:** **①** два стовпи. **②** групування **Місто vs Природа**; ⚠️ альтернатива «Розкривай (площі+лінії) vs Збирай (точки)» — **pivot-кандидат** за даними retention (не викидати). **③** перемикання **гібрид**: авто-кадр за tettsted (реюз P20) + **м'яко** (обидва стовпи завжди рахуються, змінюється лише акцент UI) + **safety консервативно** (пасивна інфо поза містом завжди; повний похідний-режим — явним тапом) + **іконка режиму** (показує авто-режим, тап = ручне перемикання). **④** механіка за геометрією: **площа**=edge-матчинг (D25, є) · **точка**=proximity (нове, дешеве; вершини — тісніший R~25-30 м, без перевірки висоти) · **лінія**=«пройшов уздовж» (нове; **стежки-комбо**: заповнення кроком 10% + позначка «пройдено» при ~75% [поріг тунабельний]; «%»=**покриття**, не метри; **вулиці НЕ фарбуємо** — будинки покривають місто; стежки лише поза містом). **⑤** зарахування точок **гібрид**: авто за близькістю + опційний тап «фото/нотатка» (=D27). **⑥** дані **комбо** (як D31): відкриті ЗАРАЗ (стежки=Kartverket **Turrutebasen**; піки=Stadnamn/OSM; POI=OSM+Kulturminne+Naturbase+Wikidata; хижки=DNT/Nasjonal Turbase) + **Friluftsrådet-партнерство** у roadmap (куровані turmål + легітимність + спонсорство [Sparebanken→StikkUT-прецедент] + FKB). ⚠️ **НЕ інжестити куровані бази Stikk UT/MoroTur** (sui generis database right + ToS) — «є в turmål» як сигнал якості лише через партнерство / відкриті проксі (Wikidata/Kulturminne) / ручну курацію свого регіону. **⑦** курація **гібрид**: алгоритм-фільтр (тип+назва+дедуп) + людина/партнер + **тірування** (куроване яскраво / сире тьмяно = declutter D28). **⑧** черга: обидва режими на **тест ASAP** (safety = **release-gate, НЕ test-gate** — тестують проінструктовані знайомі, `03` §D); порядок за складністю — міські POI → вершини/хижки → стежки+повний safety-gate (varsom/yr/SOS); **курація-адмінка → v2** (Filament, D12). **Дані-готовність verified 2026-07-12:** Turrutebasen (Geonorge WFS uuid `d1422d17`) + OSM Volda/Ørsta (22 hiking-маршрути, 2346 path-ліній, 222 піки, 540 POI) — достатньо почати; прогалини (повнота DNT-хижок, safety-шар) — перед релізом. **✅ POINTS-ONLY ЗБУДОВАНО (інкр. 1-4, 2026-07-12; компіляція+JVM-тести; рендер+картка device-verified; walk-зарахування pending польової прогулянки):** пайплайн `fetch_poi`/`build_poi` (POI з OSM, city/nature-тег, провенанс, 249 POI на R2; `--city-only` тест-allowlist = 11 міських перлин) + Android `PoiType`/`PoiIndex` + SymbolLayer (Walksy-маркери) + proximity-зарахування (Room `poi_visits`, вершини+dwell) + тап-картка (з фото) + іконка режиму. **Фото на картці:** Wikidata-клейм **P18 → Wikimedia Commons** (ілюстративне фото місця, `--images`; 4/11 міських POI: Volda/Ørsta/Hornindal kyrkje + монумент Mahler; ⚠️ **пастка:** Commons віддає **403** на дефолтний User-Agent → `StreifApp` ставить описовий UA через OkHttp-інтерсептор; **pre-release TODO:** per-image кредит автор+ліцензія — Commons=CC-BY-SA, generic «Foto: Wikimedia Commons» недостатньо на релізі, ліцензійний ризик як ODbL/CC-BY по картах). **Стежки (лінія-reveal) — окремий spike далі.** Деталі → CHANGELOG 2026-07-12. Механіки → `04` / `03` §B-D. **REFINED post-codex-review (2026-07-12; незалежний codex-огляд, 14 знахідок, verdict «не готово до повного режиму» — прийнято):** v1-природи **звужено до points-only**: (a) **стежки (лінія-reveal) відкладено** в окремий spike — складність недооцінена (сегментація/GPS-дрейф/персистенція сегментів [D30]/out-and-back/анти-чит), цінність недоведена; (b) стовпи **НЕ рівні в v1** — **reveal головний, collect другорядний** (дорости до рівних за retention-даними); (c) **POI і в місті, і на природі**, але місто — **легко/куратно** (кілька перлин, тірування, без клатеру D28); (d) **іконки типу на мапі + мінімальна калм-кор картка** (фото+назва+бейдж типу+«розкрито»+опц. lore-лінк P25; рідкість/таймери/челенджі → opt-in шар D33, НЕ v1); (e) **лишаємось на повністю видимій мапі + серпанок, НЕ fog-of-war** (D1/safety — реафірмовано); (f) **курація лін** — одне джерело (OSM) + ручний allowlist Volda/Ørsta (не мульти-джерельний data-product одразу); (g) **вершини** +dwell+topo-sanity (тісний-R-без-висоти хибно зараховує дорогу/оглядову); (h) **safety на тесті** — allowlist лише безпечні/доступні місця + мінімальний disclaimer; погоду не інтегруємо (usery мають yr); повний gate (varsom/yr/SOS) — реліз; (i) **провенанс per-feature** (source/license/source_id/last_verified) з дня 1. Деталі огляду → CHANGELOG 2026-07-12. | `04`, `03`, D27/D13/D31/D28/D1/D20, P6/P10/P16 |

| D35 | **Nature-v1 наповнення + механізм режиму (лок 2026-07-18; реалізує D34 ⑧/③, дизайн-walkthrough Дениса + адверсивна перевірка).** **(A) Safety-allowlist природних POI:** на тесті показуємо лише **безпечно-доступне** — фільтр «≤60 м від пішої мережі OSM · `sac_scale` ≤ **T2** · `ele` ≤ 1000 м» (`build_poi.py --safe` + `fetch_trails.py`); відсіює 206/270 кандидатів (Slogen/Saksa/Mohns topp — T3-T5). Це **алгоритм-фільтр**, фінальний список — за людиною (`--report` → `--nature-allow`/`--block`, D34 ⑦). Повний safety-gate (varsom/yr/SOS) лишається **реліз-гейтом**; погоду свідомо НЕ інтегруємо (у користувачів є yr). **(B) Правило «POI ≠ БУДИНОК»** (замість «POI лише поза містом»): POI, що Є будинком (`source_id`=way-id) або лежить у контурі, не показуємо — будинок і так фарбується reveal-механікою (D25), інакше подвійне розкриття того самого місця. **Виняток hut/shelter** (хижка/gapahuk = ціль походу, а не фон), і рахується він **за тегами**, не за присвоєною категорією (об'єкт буває водночас `amenity=shelter` + `tourism=viewpoint`). Наслідок: усі 13 церков ідуть із POI-шару й лишаються **фіолетовими sacral-будинками**. ⚠️ Підстава — вимір, а не відчуття: PIP 53 POI × 22 645 будинків показав, що дублювання **типове, не міське** (13/13 церков = будинки, з них 8 на природі; вершини/краєвиди/статуї/gapahuk — жодного разу), тож «POI лише поза містом» лишило б сільські церкви й убило міські монументи. **(C) Безіменні POI** viewpoint/shelter/badeplass пускаємо під генеричною назвою + `name_generic` (require-name вбивав 14 краєвидів, усі 5 пляжів, 6 gapahuk — в OSM їх не підписують). **(D) Badeplass = Miljødirektoratet Naturbase** (ArcGIS REST, `f=geojson`, без ключа; **NLOD 2.0**, атрибуція **© Miljødirektoratet**) — 385 зон → 11 названих пляжів; в OSM їх практично нема (`leisure=swimming_area` по всій Норвегії = 234). **(E) ⛔ Nasjonal Turbase / DNT API — МЕРТВИЙ, більше не розглядаємо:** `api.nasjonalturbase.no` → 404, `nasjonalturbase.no` не відповідає, дев-портал не існує, ключа отримати НЕМА ДЕ; сам DNT у довідці UT.no пише, що відкритий доступ до NTB закрито. Джерело хижок = **OSM** (3 об'єкти в Volda/Ørsta — чесна стеля) + пізніше **Kartverket SSR** (`navneobjekttype=Turisthytte`, CC BY 4.0, через Geonorge-снапшот, НЕ через search-API). **Turrutebasen** (CC BY 4.0, uuid `d1422d17-…`) — для **стежок** (лінія-reveal), як джерело хижок марна (1 хижка на весь Sunnmøre). Скрейп Stikk UT/MoroTur лишається забороненим (sui generis DB-right, D34 ⑥). **(F) Режим місто↔природа = АВТОМАТИКА + зміст:** тригер — лише геометрія меж tettsted (реюз P20; стежки/висота НЕ враховуємо, бо стежок на пристрої ще нема), **невідомий стан = МІСТО** (нейтральний дефолт); **гістерезис = буфер ±100 м (мертва зона ~200 м) + 3 фікси поспіль** (`NatureModeGate`, CMP-clean, JVM-тести) — інакше на кордоні GPS-джитер перемикав би режим щофікса; пороги польово-тунабельні (як `R_FAR`/`SLACK_CEN`, D25). Режим змінює **safety-контекст** (рядок у плашці поза містом + disclaimer на картці природного POI) **+ акцент даних** (у природі POI укрупнюються, забудова блідне; обидва стовпи рахуються ЗАВЖДИ — D34 ③ «м'яко»); ручний тап-override лишається головним. ⚠️ Плашка мусить жити на **стабілізованому** режимі, а не на сирому PIP (інакше заголовок блимає, а safety-рядок стоїть — два суперечливі твердження). ⚠️ **Підписів назв POI нема свідомо:** `style.json` без `glyphs`-ендпоінта, MapLibre Native без нього текст не рендерить, а віддалений font-сервер = новий мережевий запит (заборонено) → відкритий пункт. **(G) Маркер «я» = стрілка напрямку + коло точності в МЕТРАХ** (замість синьої крапки, що зливалась із водою й badeplass-POI): власні шари, НЕ `LocationComponent` — щоб не піднімати другий location engine (GPS іде з FGS; батарея = Stage C) і не віддавати камеру; курс уже був у контролері (компас P17). Стрілка — лише коли курс відомий, інакше чесна точка; коло точності сталого розміру **на місцевості** (D29). ⚠️ Пастка: `Style.addImage` бере pixelRatio з **густини бітмапа**, а `createBitmap` успадковує густину пристрою → без `density = DENSITY_DEFAULT` маркер виходить менший за стару крапку й різний на різних телефонах. Деталі → CHANGELOG 2026-07-18 | `03`, `06`, D34/D25/D29/P20 |
| D36 | **Синхронізація прогресу з Google Drive = «Рівень 2» (Sign in with Google + App Data Folder), лок 2026-07-20; реалізує намір «авторизація+синхронізація» з дотриманням D26/D14/D30.** Рішення Дениса: для початку достатньо **Google Sign-In + Drive App Folder**, синхронізація **раз на день (в кінці дня)** + опційне «поділитись знімком». **(A) Що бекапиться:** лише агрегати з Room — розкриття / зібрані POI / закриті колекції / підсумки сесій (id·тип·час·географія·bygningstype·дистанція-скаляр). **Сирого GPS-треку НЕМА і не буде (D14):** у Room координат не існує взагалі, `diag.csv` (debug) у бекап не потрапляє; страж-тести (`model_hasNoCoordinateFields` + whitelist ключів дроту) падають на будь-якому координатному полі. **(B) Відновлення = MERGE, не заміна (D30):** `BackupMerge` — union за природним ключем, найраніший `firstSeenTs`, перше закриття колекції зі СВОЇМ знімком знаменника (P28); детерміновано відсортовано → **ідемпотентний** push (щоденний цикл може перекритись сам із собою без шкоди), і локальні розкриття переживають вхід на новому пристрої. Чиста функція, JVM-тести. **(C) Deferred auth (D26):** нічого Google на старті; вхід лише в Settings, після цінності. **Гейт = порожній `default_web_client_id`** (не окремий прапорець) → секції «Синк» нема, ніщо не падає, поки не налаштовано OAuth-клієнт. **(D) Мінімум прав:** scope **лише `drive.appdata`** (прихована per-app тека акаунта, не весь Drive); файл лежить у Drive користувача, не в нас (свідомий «середній» рівень, не власний бекенд — межа v2). **(E) Стек:** `CredentialManager`+`googleid` (Sign in with Google — актуальний шлях, старий `GoogleSignInClient` депрекований) для акаунта; `AuthorizationClient` (`play-services-auth`) для токена drive.appdata; `WorkManager` (щоденно, мережа+**зарядка** — не будити телефон у кишені, Stage C батарея важка); Drive REST на OkHttp (не важкий google-api-client). Уся Google-специфіка за інтерфейсом `BackupTransport` → ядро злиття тестується без Android/мережі. **(F) Архітектура:** `backup/` — `BackupData`/`BackupCodec` (DTO+JSON, стабільний дріт незалежно від Room-схеми), `BackupMerge`, `BackupRepository` (Room⇄DTO, транзакція), `BackupManager` (оркестр `runSync`), `DriveAppDataTransport`, `GoogleAuthManager`, `BackupWorker`. **✅ ЗБУДОВАНО + компілюється (APK) + 261 юніт-тест + адверсивний review (усі 6 інваріантів тримаються, 4 вторинні фікси прийнято: право на видалення копії, процес-лок проти конкурентних sync, `refreshAfterRestore` після відновлення, спільний OkHttpClient); device-verified НЕ тестувалось** (телефон відключено + потрібен OAuth-клієнт Google Cloud Дениса — кроки в `release-checklist` частина C). **⚠️ `applicationId`:** ~~поки лишаємо `no.streif.spike`~~ → **`app.streif`, застосовано 2026-07-29** (`3495194`, `release-checklist` §B.0). Наслідок для ЦЬОГО рішення блокуючий: Android-OAuth-клієнт прив'язаний до пари *(package name, SHA-1)*, тож усі наявні клієнти під новим id недійсні — синк із Drive падає з `DEVELOPER_ERROR` аж доки Денис не створить клієнти на `app.streif` (§B.6.3 #6). | `07`, `release-checklist` C, D26/D14/D30/P28, D12 |
| D37 | **Версіонування схеми тайлів для кеш-інвалідації (лок 2026-07-20).** Зони кешуються назавжди (D24), тож зміна СКЛАДУ властивостей фічі (`tettsted_id`→`kommune`→`bt`) лишалась непоміченою — старий кеш мовчки віддавав фічі без нового поля, фіча тихо не працювала, лікувалось лише ручною `adb`-чисткою (реліз-блокер: тестери цього не зроблять). **`build_tiles.py::DATA_VERSION` → `manifest.dataVersion`** — версія ФОРМАТУ, не свіжості (`generated` не годиться: реген тих самих даних скидав би кеш усім = зайвий трафік). `DataVersion.shouldInvalidate`: `!=` (відкат схеми теж інвалідує), `UNKNOWN`→ні; збіг = 0 трафіку. `AreaCache` неймспейсить теку версією (`areas/v2/`) — атомарне перемикання vs небезпечний `deleteRecursively` (міг би лишити СУМІШ схем); `AreaLoader.invalidate` epoch-лічильником відкидає зону, що була в дорозі. **Розкриття не страждають (D30):** чистимо лише тайли, лічильники в Room, малюнок відновлює `reconcilePersisted`. | `05`, D24/D30 |
| D38 | **Локального «видалити всі мої дані» НЕ буде — видалення застосунку як канонічний шлях (лок 2026-07-21, рішення Дениса).** Кнопку збудували в пакеті A, і **три раунди adversarial review** показали справжню ціну чесної обіцянки «все, що застосунок зберіг на телефоні»: Room + файлові артефакти + кеш зон (`areas/`, **координати в іменах файлів**) + ambient-кеш MapLibre (`mbgl-offline.db`, ~50 МБ, кожен URL тайла) + гонки з асинхронною репопуляцією (`init`/`refreshAfterRestore`) + зупинка активної прогулянки + маркери проти воскресіння з Drive (union-merge D30 повертав стерте при наступному вході). Кожен раунд закривав дірки й відкривав нові — ознака не поганої реалізації, а того, що фіча чіпає забагато підсистем. **Рішення прибирає весь клас проблем, а не обходить його.** Лишається: «Видалити копію з Google Drive» + «вимкнути бекап» (те, що вийшло за межі пристрою, видаляється в застосунку). Локальне стирається **видаленням застосунку** — і це НАДІЙНО, бо `AndroidManifest:18` має **`android:allowBackup="false"`**: ОС стирає приватну теку й **не відновлює** її при перевстановленні (якби стояв дефолтний `true`, обіцянка була б неправдою, ще й з недекларованим потоком даних до Google). Той самий шлях **уже був описаний** у політиці приватності §7 — тобто застосунок приведено у відповідність до опублікованої обіцянки, а не навпаки. **D30 лишається без винятків** — саме винятки з нього й були джерелом складності. Play Data safety «request deletion» = **Yes** (§4.1). ⚠️ Втрачаємо «почати з нуля, не видаляючи застосунок» — це **продуктова** фіча, не вимога відповідності; якщо колись знадобиться, робити свідомо, знаючи ціну. | `07`, `play-data-safety` §0/§4.1, `release-checklist` §B.6, D14/D26/D30 |
| D39 | **Телеметрія закритого тесту — знеособлені агрегати на власний Cloudflare Worker, default-ON + екран згоди при першому запуску, з планом прибирання (рішення Дениса 2026-07-27).** Мета — не «аналітика», а відповіді на конкретні відкриті питання проєкту, яких з одного телефона в одному місті не отримати: (1) **якість матчингу D25 поза Volda** — тап «тут неправильно» на розкритому будинку; (2) **батарея й виживання FGS на чужих OEM** (Stage C пройдено лише на Pixel; Samsung/Xiaomi вбивають фонові служби по-своєму); (3) **здоров'я даних** — невдачі зон/тайлів, промахи manifest, 403 Wikimedia, **скільки файлів копії бачив синк** (валідує фікс §B.6 #13 у полі); (4) **retention P10** — те саме «МІРЯТИ, не приймати аксіомою»; (5) падіння/ANR — але їх дає **Play Console Vitals безкоштовно й без коду**, тож самотужки не збираємо. **Обсяг — варіант «в»: повні агрегати + груба геоприв'язка на сесію (tettsted).** ⚠️ **Умова, за якої це ухвалено:** «тестую спочатку серед друзів» — коло тестерів відоме особисто. **При розширенні тесту поза це коло геополе переглянути окремо**; безумовним воно не є. **D14 не порушено:** назовні йдуть лічильники й гістограми, ніколи не точки — ні координат, ні послідовності `building_id`, ні часових міток розкриттів, тобто маршрут не відновлюється. **Транспорт — власний Cloudflare Worker → R2** (ndjson), а не Firebase: R2 і домен `streif.app` уже є, нових залежностей у застосунку нуль (OkHttp є), дані лишаються в нас, а не в третьої сторони; аналіз — Python поруч із `spike/fieldtest/analyze.py`. **Згода — default-ON + екран при першому запуску** (що надсилається · навіщо · де вимкнути) + перемикач у Settings: мовчазний default-ON у Play — ризик того ж класу, що вже ловили в disclosure локації, а чистий opt-in дав би замало даних. **Ідентифікатор** — випадковий UUID на встановлення в prefs, НЕ прив'язаний до акаунта, зникає з видаленням застосунку. **Архітектура під прибирання** (вимога Дениса «потім приберемо»): один модуль `telemetry/`, один `BuildConfig.TELEMETRY`, один ключ `SettingsStore`, одна секція Settings — як зроблено з наборами іконок. ⚠️ **Тим самим пакетом оновити документи, інакше вони брехатимуть:** політику приватності, `play-data-safety.md`, prominent disclosure і `play-fgs-declaration.md` §2.1 (він каже «no analytics») | D14/D20/D26/D30/D36, P10, `07`, `play-data-safety`, `release-checklist` §B.6 |
| D40 | **Незалежне cross-model review після кожної великої зміни — постійне правило, не разова акція** (рішення Дениса, 2026-07-31). Було: review запускалось точково на гейтах (**D23** — межа Spike-2 → MVP-0; **D23.1** — виконано 2026-07-01) і час від часу на запит. Тепер це **штатний крок**: велика зміна (нова механіка, зміна моделі даних, зміна того, що йде в мережу, новий шар) → **codex-review** → **пропозиція власнику, що варто змінити**, на основі його висновків. Проєкт до цього вже пристосований: `AGENTS.md` — єдине джерело контексту, яке codex читає напряму, а `CHANGELOG.md` веде саме «для стороннього review (codex) і згадування».<br>⚠️ **Знахідки codex НЕ приймаються на віру** — вони верифікуються по коду, і в цьому весь сенс кроку. Підстава з досвіду, не з принципу: у **D23.1** із 18 знахідок підтвердилось **16, а 2 спростовано**; у сесії 2026-07-12 із 14 знахідок частина звузила скоуп Nature-v1 до points-only (правильно), але приймалась вона теж після перевірки. Тобто вихід кроку — **синтез**, а не пересилання списку.<br>⚠️ **Не лише код.** Практика показала, що найцінніше codex дає **до** розробки — на дизайні (звуження D34 → points-only) і на правилах. Тому review доречне і для продуктових рішень, не тільки для дифів.<br>🔧 **Операційне — модель і рівень міркування (рішення Дениса 2026-07-31): модель `sol`, міркування `xhigh` (найвищий) або `high` (передостанній).**<br>Робоча команда:<br>`codex exec --sandbox read-only -m gpt-5.6-sol -c model_reasoning_effort='"xhigh"' - < brief.md`<br>⚠️ **Вимагає свіжого CLI.** На **0.101.0** `sol` віддавав `ERROR: The 'gpt-5.6-sol' model requires a newer version of Codex` — і це **не видно як помилка моделі**, запуск просто нічого не повертав. Оновлено до **0.146.0** (`npm install -g @openai/codex@latest`), перевірено: заголовок сесії показує `model: gpt-5.6-sol · reasoning effort: xhigh`.<br>⚠️ **Пастка, що коштувала першого review:** у `~/.codex/config.toml` вже стояло `model = "gpt-5.6-sol"`, але поруч — `model_reasoning_effort = "low"`. Тобто модель була прописана правильно, а рівень міркування мовчки лишався **найнижчим**; на дизайн-критиці це дуже помітно. **Рівень задавати прапорцем явно**, не покладатись на конфіг. Допустимі значення: `none · minimal · low · medium · high · xhigh` (`max` — НЕ валідний варіант, саме на ньому кеш моделей кидає нефатальну помилку).<br>⚠️ **Не питати модель, хто вона.** На пряме питання `gpt-5.6-sol` назвалася «GPT-5.4» — самоідентифікація моделей ненадійна; авторитетний лише заголовок CLI.<br>Кликати з кореня docs-репо, щоб підхопився `AGENTS.md`; у брифі окремо перелічити, **які дані реально є**, інакше пропонує механіки під неіснуючі поля | D23/D23.1, `AGENTS.md`, `CHANGELOG.md` |
| D41 | **Нагорода дається за ЗАКРИТТЯ НАБОРУ — не за дію і не за кількість** (правило ухвалив Денис 2026-07-31; сім рішень однією сесією, після двох проходів незалежного codex-review за D40). Це не нова механіка, а **єдине правило**, з якого виводяться всі решта: питання «за що давати нагороду» зводиться до значно вужчого «які набори ми вміємо чесно порахувати». Складність виражає **структура набору** (D13), а не вага.<br>⚠️ **Головне, що з'ясувалося при звірці: механізм уже збудований і живе в тестовій збірці** (`ui/Collections.kt`, `CollectionsSection.kt`, секція «Samlinger» у шторці прогресу). І він уже реалізує половину «нових» гардів, які запропонував рецензент, подекуди жорсткіше: знімок знаменника при закритті (`CollectionClosure.denominator` + гард у `state`) · нові об'єкти після закриття = **бонус**, не пониження (`newSinceClosed`) · заборона вироджених наборів (`MIN_SIZE = 5` — жорсткіше за «`N=1` заборонити») · чесність про покриття кодами (`classCoverageComplete` + `unknownAccessible` → «усі **відомі** naust») · **гард видимості** (`featured` = лише закриті й близькі; «перелік того, чого ти НЕ зробив, — це тиск, а не спокій»). Відбір наборів **правилом, а не списком**: `CollectionRules` генерує їх із пари «одиниця × клас» і фільтрує за масштабом `5..1000`, а назви кодів дає згенерований `bygningstype.json` — **ручної курації не потрібно ніде**.<br>**Сім ухвал:**<br>**(1) `capped ×2` знято** — стеля, виражена через множник, суперечила сусідньому «rarity НІКОЛИ не множник». Заміни не треба: у даних **112 кодів `bygningstype`** із хвостом до однієї будівлі, тож рідкість уже виражена структурою.<br>**(2) Бейдж «Balansert utforsker» викинуто** — «баланс» є співвідношенням/порогом, тобто нагородою не за закриття набору; варіант «у періоді» в `04` додавав ще й часове вікно проти D20. Баланс лишається вираженим **complementary-колекцією**, що вимагає обох стовпів.<br>**(3) Заморозка складу набору — лише в момент закриття** (як було в P28; альтернативи «при першому членові» й «при явному почати» відхилено). ⚠️ Ціна прийнята свідомо: **до** закриття ціль може рухатись від оновлення даних. Компенсація — **інспектовний знаменник**: показувати, з якого знімка він узятий, і не приховувати, що він може зрости.<br>**(4) «Перші кроки» (§5) лишаються нагородами в альбомі** — попри те, що це набір розміру 1, тобто формально нагорода за дію. ⚠️ Виняток **названо винятком і обмежено закритим списком**; він не поширюється ні на що інше. Наслідок для UI: **альбом містить картки двох видів** — зі знаменником і без, — і сітка має вміти обидві.<br>**(5) Штамп у P22 — нейтральна позначка члена, а не нагорода; нагорода = закрита сторінка.** Причина не естетична: якщо штамп дає нагороду, їх число дорівнює числу відвіданих місць, тобто єдина шкала повертається з іншого боку — рівно те, що відхилено в (7).<br>**(6) Додаємо `lastSeenTs`** у `visits` **і** `poi_visits` (міграція Room 4→5). Без нього мертві **сезонні листівки** (повторний зимовий візит ніде не фіксується) і P19 «свіжість» — обидві сутності мають лише `firstSeenTs`. ⚠️ Тягне **нове правило злиття бекапу**: `BackupMerge` бере найраніший `firstSeenTs`, для `lastSeenTs` треба протилежне (найпізніший), не порушивши D30. `passedCount` у `sessions` **не додаємо** — механіку «прогулянка без повторів» відхилено як таку, що не є набором.<br>**(7) Наступний крок — альбом (P22) поверх наявного механізму**, не нові набори. Тобто робиться «як виглядають», бо «за що» вже працює.<br>**Відхилено з обґрунтуванням:** мета-нагорода «зібрано N нагород» (прихований score: зводить різнорідні закриття в одну шкалу й штовхає оптимізувати **дешеві** нагороди; якщо колись треба другий поверх — лише іменовані скінченні над-набори, не лічильник) · відсоткові пороги 10/25/50% (це шкала, а не закриття множини місць) · «квартал/вулиця цілком» (у тайлі є рівно `building_id · type · accessible · bt · kommune · tettsted_id` — вулиці немає) · «прогулянка без повторів».<br>**Відкладено до нових даних:** «острів цілком» (полігони N50) · «nabokommune pair» (графа суміжності комун немає) · «boligformer»/«hverdagssteder» (вимагають кураторського зіставлення `bt`-кодів зі слотами).<br>⚠️ **Лишається відкритим у коді:** `CollectionCatalog.closedCount` підписаний «єдине зведене число, яке ми тут дозволяємо» — це і є «зібрано N нагород», відхилене в (7), просто вже написане. Плюс немає **відбитка означення набору** (`definitionVersion`: однакове `N` не доводить незмінності складу), окремого стану **reconciliation** (набір закрився через вилучення члена, а не через людину) і **між-одиничних наборів** | D13/D20/D30/D33, P19/P21/P22/P27/P28, `04` §2/§4/§5/§11, D40 |

| D42 | **Категорії будівель = 11 змістових + `Andre` (G1, лок 2026-08-02, рішення Дениса).** Обґрунтування нинішніх 6 **не існувало ніде**; тепер є правило відбору: групи SSB KLASS #31 → гейт «≥5 ДОСЯЖНИХ у кожній із 27 комун» → злиття за роллю (`08` §15.2). Ухвалено варіант власника «Semden 12» + **дві заміни** за незалежним review: прибрано окремий `Rekkehus` (роль ближча до звичайного житла), відділено `Industri/energi` від `Driftsbygg` (2 554 будівлі, 27/27 комун, і саме промислова сітка пережила спайк візерунків). Три ухвали власника, яких не було в жодному згенерованому варіанті: окрема `Helsebygning` · торгівля поділена за наміром («купити» vs «обслужать») · гангари до гаражів, а не до складів. **Перевірено двічі незалежно** (жива копія продакшн-маніфеста): 129 кодів рівно, перетинів нуль, 226 777 усього / 196 254 досяжних; 9 категорій 27/27, `Handel/service` 26/27, **`Helsebygning` 21/27 — свідомий виняток** за змістовою вагою й перший кандидат на злиття. Єдине джерело — `BuildingCategory.kt`. ⚠️ **Застосування ділиться на три рівні** (`08` §15.11.4), і заморозку §B.7 порушує **лише (в) перезбірка тайлів**, яка не потрібна: тайл уже несе сирий `bt`, категорія рахується на клієнті. Попередній запис у каноні «зміна категорій заблокована заморозкою» був **хибний**. | `08` §15.11, D28/D6 |
| D43 | **Палітра будівель = варіант A — 12 кольорів + обов'язкове обведення (G1, лок 2026-08-05, рішення Дениса на пристрої).** Закриває P34. Порахована палітра §15.10 була відхилена за провал під дальтонізмом (мін. ΔE 1,86); A віддає межу обведенню, тож заливка не мусить сама тримати 3:1 — правило §15.11.3 №3 задовольняється гілкою «АБО обведення». Виміряно на 66 парах: normal **17,83** · deutan **10,58** · protan **10,49** · tritan **8,39**.<br>**Єдина правка до виміряного A — `Bolig` #617F11 → #3B6A07** (оливковий → трав'яний). §15.11.2 записало «оливковий» як нелікувану ціну deuteranopia; перебір коридору це **спростував**: трав'яний hue 126 тримає ті самі числа на дванадцятці, проти POI кращий у КОЖНОМУ режимі (protan **14,17 проти 5,35** — оливковий сидів упритул до золотого `poi:hut`) і єдиний зі змін **знімає контрастний недобір** (3,52 проти 2,52). Претензія «`Sakral` став синім» **не лікується** і лишається свідомо: коридор, що проходить правило, — hue 289-307, справжній пурпур валить дванадцятку до 12,07/4,87.<br>⚠️ **Три виміри, яких первинна перевірка не робила** (§15.11.7), закрито разом із ухвалою: контраст до **найтемнішої** ділянки підложки, а не до «паперу» — недоборів **7 із 12**, не 3 (для A це не провал, а саме та гілка правила) · режим «природа» (`fillOpacity` 0,55) — deutan падає до **5,80**, тобто там колір перестає нести тип (відома межа, не «ок») · **об'єднаний набір із POI (19 кольорів)**, якого не міряв ніхто: сьогодні ТРИ пари POI-кольорів збігаються з будівельними **байт-у-байт** (`shelter`=`housing`, `church`=`sacral`), A це прибирає (normal 0,00 → **9,03**).<br>⚠️ Залишок 0,25 у об'єднаному наборі — `viewpoint ↔ peak`, тобто **POI ↔ POI**: він був до палітри й від неї не залежить → окреме відкрите питання по `PoiType`.<br>Єдине джерело — `BuildingCategory.kt` (колір живе на самій категорії). Перемикач порівняння знято цілком; історія вимірювання лишилась там, де її можна **перевірити**, а не переписати — `spike/palette/palette_check.py` (`--palette=` знає всі чотири набори, `--with-poi`, `--opacity=`). | `08` §15.11.2/§15.11.8, D42, P34 |
## Замінені (superseded)

| # | Було → стало |
|---|---|
| S1 | Laravel + PostGIS як MVP-стек → **D12** (CLI build-пайплайн; backend на v2) |
| S2 | `feature-state` рендеринг (`03` §A) → **D7** (двошаровий overlay) |
| S3 | «FKB/INSPIRE — CC-BY 4.0» (CLAUDE.md) → **D8**, уточнено **D31** (INSPIRE не покриває регіон → продакшн-геометрія = OSM self-host ODbL + Matrikkelen; FKB/INSPIRE — потім через покриття/партнера) |
| S4 | «Немає Android-застосунку для щоденного дослідження» (`01` §2) → **D2** (Fog of World — на Android; CityStrides — веб; Walkable — iOS) |
| S5 | Негайне видалення сирого треку (`07`) → **D14** (локальний diagnostic 30 днів) |
| S6 | «Формули балів / difficulty» (`03` §E) → **D13** (без score; три виміри) |
| S7 | D7 `withSynchronousUpdate(true)` → **async (false)** — sync ≈2× латентність + колапс fps при оновленні під час пану (spike D22) |
| S8 | Фіксований мульти-сіті бандл будинків (стопгап Vinstra/Torpo/Enger/Oslo) → **D24** (on-demand area download; bbox не масштабуються під рух користувача) |
| S9 | Матчинг до **центроїда** будинку (within-R, диск+сегмент, D5) → **D25** (edge-distance до контуру + closest-approach gate; центроїд спричиняв і «зарано», і пропуск через дорогу/видовжених — польовий присуд) |

## Відкриті (proposed)

| # | Питання |
|---|---|
| P1 | **Назва «Streif»** — лишити робочою; до логотипа: Navnesøk (домен + foretaksnavn + trademark) · тест розуміння на ~10 норвежцях · searchability в сторах (колізія з австрійською трасою Streif) |
| P2 | **Фінальні пороги матчингу** — частково вирішено польовим присудом D25 (edge-метод + R_FAR=18/MIN_EDGE=8/SLACK=1.5, ACC_MAX=30). **Лишається тюнінг `R_FAR` (18↔20)** на ширші вулиці — 6/15 пропусків ще за межами 18 м; збирати ще ✓/✗-мітки (маркування лишити ON) і переганяти `analyze.py`. Пороги перенавчені на 1 трасі (Volda) → переперевірити на щільнішій забудові. **⚠️ D23-review (2026-07-01): НЕ тюнити `R_FAR` на поточних даних — recall спотворений двома багами: (а) тангенційний промах (eligibility-вікно `edge≤R_FAR` закривається перш ніж центроїд виросте на SLACK_CEN — будинок випадає з кандидатів → `passedMiddle` не спрацьовує; корінь Ristevegen 1), (б) cold-start: буфер replay 12≈24с < часу Overpass-fetch → ранні фікси витісняються. Спершу розчепити eligibility/timing-вікна (тримати `runMin`-оцінку кілька фіксів після виходу за R_FAR) + розширити replay-буфер за часом/дистанцією, ТОДІ чиста повторна прогулянка. Справжнє розрізнення «вздовж 15м vs через дорогу 15м» — це D6 (піша мережа), не поріг.** **Присуд 2026-07-04 (чистий walk на реальних фіксах):** recall 162/164 (99%) живцем, пропуски ↓3×; пороги на оптимумі — жодна зміна R не покращує (R=16…22 → хибних 7/7); тайминг `SLACK_CEN 2→1` (промптніше за фідбеком, без цінності recall/precision). Лишається ТОЧНІСТЬ (across-road/«зарано») — окремий алгоритм + більше даних, не поріг. «Пізно після проходу» (p90 ~10м) = холодний старт зони → префетч наперед. |
| P3 | **Джерело меж для Coverage-%** — ✅ **вирішено P20** (SSB Tettsteder; grunnkrets дрібніше пізніше). *(Було: bydel/grunnkrets — Volda/Ørsta не мають офіційних bydeler.)* |
| P4 | **Ліцензія footprint INSPIRE + офлайн-копіювання** — письмово підтвердити з Kartverket |
| P5 | **v2 backend** (Laravel vs Supabase vs Django) — обрати коли scoped |
| P6 | **Пройдені вулиці/маршрути як доповнювальний шар** (CityStrides-стиль, але «особиста спокійна мапа», не змагання) — оцінити **після** spike будинків; поки **не додаємо** (2026-06-21) |
| P7 | **feature-state на Android** — стежити за PR #4219 (issue #1698); якщо приземлиться у 13.x/14.x → оцінити **одношаровий** recolor тайлованої бази замість overlay (стабільний `building_id` уже готовий) — D22 |
| P8 | **Zoom-важіль overlay** (ідея Дениса) — `overlay maxzoom` → агрегат на дальньому зумі (+ опц. `minZoom`), виставити під виміряну стелю; на Pixel 9 не обов'язковий (60 fps@4000), але страховка для mid-range; діє на overlay, не на тайловану сіру базу — `08` §9, D22 |
| P10 | **Двигун «нові місця» + утримання в малому місті** (ключова потреба Дениса, 3× у фідбеку) — як спонукати відкривати нове, але щоб юзери малих міст не закинули після «100%». Напрям (не суперечить D1/D13/D20): (а) опц. **свіжість/серпанок** — розкрите бляне без повторного візиту, але **НЕ зменшує lifetime-стат** (відповідь на топ-побажання Fog of World); (б) **Variety/Discovery** (D13) дають цілі поза Coverage-% навіть у малому місті; (в) **природний шар** (стежки/вершини/turmål) = «нескінченний» контент навколо малих міст; (г) сезонні/часові стани + живa мапа (нові дані). Heatmap-інтенсивність — post-MVP (`09` §5). **Прототипувати й МІРЯТИ retention, не приймати аксіомою** | `04`, `01` §9, D2/D13 |
| P13 | **М'яка гейміфікація — відкрите** (Денис ще обмірковує, 2026-06-26) — (а) **повнота колекцій у числовому вигляді** (X/Y типів, X/Y районів) — це спокійний прогрес-лічильник, НЕ абстрактний score → сумісно з D13; (б) ідея **щоденної колекційної нагороди** на випадковій будівлі на пішій відстані, **в межах міста користувача** (простіший варіант — не треба зберігати «часте місце старту»; навіть якби зберігали — це device-local, не порушує D14). ⚠️ Guard: opt-in, **без streak-покарання/expiry** (D20). **Тверда межа (Денис 2026-06-27): НЕ примушувати ходити щодня й НЕ штрафувати за пропуск — будь-яка щоденна механіка лише як необов'язковий подарунок.** Синергія з P10 (нагорода віддалік = стимул до нового). *Імпорт історії GPX/Timeline — знято (не потрібен, рішення Дениса).* | `04`, D13/D20, P10 |
| P16 | **Авто-маршрут до недослідженого (v2+)** — застосунок САМ пропонує маршрут, що максимізує нову територію; користувач задає **довжину (час або відстань)** і **тип (в один бік / кільцевий)** (фіча-запит Дениса #9). Обчислювально важко (Chinese-postman / max-new) → v2+; підсилює reframe «новий маршрут» (D2) | `10` §11, D2 |
| P17 | **Орієнтація мапи «за напрямком руху» (bearing-up)** — фіча-запит Дениса (як автонавігатор). **Фіксовано:** опційний перемикач, **дефолт north-up** (для пішохода course-up дезорієнтує сильніше, ніж в авто — зупинки/розвороти/півоти на місці; Walkable-подібні тримають north-up + напрямлений puck). **Відкрите:** джерело курсу — **компасний heading (куди дивишся, `ROTATION_VECTOR`)** vs **GPS-course (куди йдеш, `Location.bearing`)**; для ходьби компас природніший, але шумніший у приміщенні/біля металу → можливо дати обидва або авто-вибір. **Реалізація:** швидкий спайк у наявній камері (`CameraPosition.bearing` + puck `SymbolLayer.iconRotate`; згладжування обов'язкове — мертва зона <~0.8 м/с, тримати останній курс, кутовий low-pass через 360°) АБО (чисто, MVP-1) MapLibre `LocationComponent` (`RenderMode.COMPASS`/`GPS` + `CameraMode.TRACKING_GPS`) — заразом прибирає ручний me-шар + `easeCamera`. **Реалізовано (спайк) 2026-07-04:** компасний перемикач north-up↔course-up (іконка 🧭 справа-центр, ROTATION_VECTOR + кутове згладжування), verified на пристрої. Обмеження: азимут без tilt-remap (найкраще тримати телефон пласко); чиста версія (LocationComponent / fusion GPS-курс+компас) — MVP-1 | `08`, `10` §8 (фаза 5) |
| P18 | **Per-type / загальні по місту тотали (region-manifest)** — on-demand (D24) не дає «скільки всього N типу в місті» / точного знаменника; порахувати в CC-BY-пайплайні (`spike/pipeline/`) підсумковий файл на регіон (скан тайлів Volda/Ørsta → лічильники по `bygningstype` + accessible-тотал), покласти на CDN, застосунок тягне раз. Живить: тап-по-іконці типу в плашці «відкрито X з Y» (D28 / `08` §8) + правильний загальний Coverage-% (замість «від завантаженого поблизу»). Мала data-задача (Python), не UI. **РЕАЛІЗОВАНО 2026-07-06** (пайплайн-лічильники + Android fetch/показ + реальний manifest на R2, device-verified: «X of Y» + city-%). Регіон = **КОМУНИ** 1577 Volda + 1520 Ørsta (не міста) — тому числа великі (22 645 буд., 87% досяжних; сільські uthus/hytter уключено). **⚠️ Нац.масштаб:** ОДИН глобальний manifest зламає метрику (знаменник = уся Норвегія → «X з мільйонів», Coverage ~0%) → потрібні **per-region manifests** (per kommune, у великих містах per bydel), застосунок обирає за локацією (GPS→kommune / tile→kommune); пайплайн уже per-kommune, тож емісія per-kommune-manifest природна. Скоуп «своєї території» (kommune vs bydel vs радіус) — рішення при масштабуванні, дотично P3/P10. Пов'язано з P3 (знаменник %), D24 |
| P19 | **«Свіжість» — розкрите тьмяніє без візитів, прохід освіжає** (відповідь на «що робити, коли проходиш повз уже розкрите», Денис 2026-07-06; ЗАРАЗ — варіант A «нічого», розкрите лишається розкритим). Розкрите, куди давно не заходив, поступово **десатурується** (гуе зберігається — принцип 08 §2; це «серпанок» D28 §3), а прохід повз **освіжає** (знову насичене + легкий glow). Дає позитивне підкріплення за повернення + **причину ходити знову** (двигун утримання в малому місті, P10). ⚠️ lifetime-«розкрито» НЕ падає — без штрафу/expiry (D20/D30). **Більша фіча → ПРОТОТИПУВАТИ Й МІРЯТИ** (мотивує чи тисне?), у раунді утримання/гейміфікації, не зараз. Пов'язано з P10, D28 §3, D20/D30 |
| P20 | **Area = tettsted (динамічна поточна) + видимий кордон** (рішення Дениса 2026-07-06, з walk-фідбеку: «0,7% of area» абстрактне без кордону + area завелика = обидві комуни Volda+Ørsta, 19 723 буд.). Coverage-% рахується від **tettsted (міське поселення), у якому ти ЗАРАЗ** (SSB Tettsteder — ліцензія **NLOD**, деталі нижче; НЕ CC-BY), не від комун; **кордон поточного tettsted малюється лінією** на мапі (area стає видимою, не словом; принцип §1 самопояснювальність). При масштабі — кордони **сусідніх tettsteder** теж видно. Пізніше (з системою досягнень) — можливо дрібніше, **grunnkrets**. **Обсяг:** пайплайн (SSB tettsted-межі → per-tettsted лічильники по типах + тег `tettsted_id` на кожну будівлю + boundary-GeoJSON) + застосунок (GPS→tettsted point-in-polygon → per-tettsted %, boundary-шар). **Поза будь-яким tettsted (село / між містами) = «поза містом» без %** (варіант a, Денис 2026-07-06). Розширює P18 (per-region → per-tettsted manifest) + P3 (знаменник = tettsted). **✅ ПОВНІСТЮ В ПРОДАКШНІ 2026-07-11 (пайплайн+Android+R2, end-to-end verified):** data-спайк → джерело **SSB Tettsteder WFS** `kart.ssb.no/api/mapserver/v1/wfs/tettsteder` (`ms:tettsted_2025`, стабільний id `tett_nr`, EPSG:4326, geometry без дірок; Volda 6071 + Ørsta 6081 покриті); **⚠️ ліцензія NLOD, НЕ буквально CC-BY** (відкрита/атрибуція, сумісна; кордони — окремий файл, не в ODbL-тайлах → +© SSB в атрибуцію). Пайплайн: `fetch_tettsteder.py` + `build_tiles.py --tettsteder=` тегує `tettsted_id` (centroid-PIP) + емітить `tettsteder.geojson` (межі + per-tettsted `total`/`accessible`/`byType`; лише data-backed). Android: `TettstedIndex` (CMP-clean PIP+тотали) + `TrackingRepository` (чисельник per-tettsted **із тегу `tettsted_id`** — index-незалежний; `currentTett` GPS→PIP) + `StatusPlate` «% від <tettsted>» / «поза містом» (гейт `tettLocated`) + `MapController` два line-layer (сусіди пунктиром + поточний насичений; feature-state Android нема D7). ⚠️ після регену очистити `filesDir/areas` (старі тайли без тега). **Рішення Дениса 2026-07-11:** кордон — **ЛИШЕ поточний** tettsted (без сусідів); лічильник Revealed — **ЗАВЖДИ глобальний** (per-tettsted лишається тільки %); «поза містом» — нейтральний. **End-to-end верифіковано (Pixel):** повний реген (Matrikkelen+OSM+Elveg+Tettsteder, 416 тайлів, accessible 87.1%) залито на **R2**; очищено кеш → застосунок дотягнув тайли+межі з CDN → плашка «Revealed 222 (**6,2% of Volda**)» (реальний per-tettsted % = 222/3606), теал-кордон Volda видимий, © SSB, без крешу. Деталі → CHANGELOG 2026-07-11. Пов'язано з P18, P3, P10, `08` §1 |
| P21 | **Обʼєм (Площа×Висота) — ціль-так, формула-ні** (з брейнсторму `04-2`, 2026-07-11; opt-in шар D33) — мета «баланс місто↔природа» варта, але **мультиплікативна формула/валюта відхилена** (реінтродукувала б score — S6/D13; degenerate case «місто-only→Обʼєм 0» = докір моно, проти D20). Замість: (а) дві **незалежні шкали** (Coverage + висотні метри/вершини, вже в D13/`03`); (б) **complementary-колекції**, що вимагають обох («Utforsket Volda» = tettsteder + топи навколо); ~~(в) опц. бейдж «Balansert utforsker»~~ — **ВИКИНУТО 2026-07-31 (D41):** «баланс» є співвідношенням/порогом, тобто нагородою не за закриття набору. Баланс лишається вираженим через (б). Сам «Обʼєм» — щонайбільше **декоративний візуал** двох незалежних шкал (без числа/ґейтингу нагород). Post-MVP, opt-in | `04`, D13/D20/D33 |
| P22 | **3D-діорама «мініатюрна Норвегія»** (з `04-2`; opt-in шар D33) — колекція вирощуваних landmark-ів (stavkyrkje/fyr/вершини) як особистий світ; сильний диференційний образ, але real-time 3D = велика нова поверхня для solo-dev + native-OOM ризик MapLibre при динамічних шарах (D32). **Старт: 2D-«samling»** — конкретна форма = **штамп-альбом** (унікальний штамп на місце + альбом; культурно перевірено — япон. стемп-ралі YAMASTA, Stikk UT!-кодові ящики), ≈80% ефекту без 3D. ⚠️ **Це НЕ заміна 3D, а перша фідельність тієї самої колекції** (штамп → пізніше 3D-мініатюра); **3D лишається цільовою формою — пріоритет Дениса (2026-07-19)**, після 2D-прототипу + підтвердженого retention.<br>⚠️ **Уточнено 2026-07-31 (D41): штамп — НЕЙТРАЛЬНА позначка члена набору, а не нагорода; нагорода = ЗАКРИТА сторінка альбому.** Формулювання «унікальний штамп на місце» читалось як нагорода за кожне місце — а тоді число нагород дорівнювало б числу відвіданих місць, тобто єдина шкала повернулась би з іншого боку (те саме, за що відхилено «зібрано N нагород»). Емоційний пік стоїть на **рідкісному завершенні**, не на кожному кроці.<br>**Альбом — наступний крок у роботі** (D41 п. 7): механізм закриття наборів уже збудований (`ui/Collections.kt`), бракує саме візуальної форми. Post-MVP | `04`, D28/D32/D33 |
| P23 | **Типи будівель = «точки дій»** (ідея Дениса 2026-07-11; opt-in шар D33) — тип надає функцію, не лише колір (напр. релігійна будівля → конвертація відкритого в діораму). Гарди: **без хард-гейту прогресу за рідкісним типом** (rural-щільність — анти `09` §11 «порожня мапа»); тактовно/безпечно (підхід з публічного простору D6; повага до діючих церков); мапінг тип→дія осмислений, короткий, опційний. Зчеплено з P22. Post-MVP | `04`, D6/D28/D33 |
| P24 | **Новизна маршруту («нова вулиця сьогодні»)** (доповнення 2026-07-11) — спокійний сигнал, що прямо інструментує центральну метрику успіху **D2** (частка нових сегментів проти повторних; «рухайся інакше знайомим світом»). **Залежить від шару «пройдені вулиці/сегменти» = P6** (поки відкладено) → реалістично fast-follow/post-MVP разом з оцінкою P6. Пасивний відповідник активного авто-маршруту P16; без балів | `04`, D2/P6/P16 |
| P25 | **Історичний шар реальних місць** (з `04-2` §8; post-MVP контент) — відвідав місце → розкриваєш його історичний шар (старі фото/карти, що тут було 100–200 р. тому). Унікально, освітньо, sponsor-friendly. Дані значною мірою відкриті: Riksantikvaren/Kulturminnesøk, Nasjonalbiblioteket (PD-фото/карти), Kartverket historiske kart, lokalhistoriewiki. Радше контент/освіта, ніж механіка; концептуально біля **D27** (POI+нотатки природного шару). Post-MVP | `06`, `04`, D27 |
| P26 | **In-app налаштування / вигляд (Settings-екран)** (ідея Дениса 2026-07-12, з тюнінгу стилю карти) — винести tuning-опції в екран, щоб Денис перемикав сам на пристрої **без ребілду** (пришвидшує власну design-ітерацію фази dogfood: замість «Claude пересобрав+встановив» → «тапнув і порівняв»). Кандидати: **(a) стиль базової карти Kartverket** (`topograatone`/`topo`/`toporaster` + опц. приглушений `topo` через MapLibre `raster-saturation`) — **мале**, лише swap raster-source у `MapController`/style + персист; **(b) кольори типів будівель** — color-picker per тип; ⚠️ живить І мапу (fill-expression `MapController`) І легенду (Compose `BuildingPalette`) + персист + reset-to-default → це **не лише зручність, а accessibility-фіча** (colorblind-користувач обирає власну палітру, WCAG-aligned, `08` §2) — **середнє, окремий інкремент**; **(c) акцент/фон кнопок (FAB)** — мале. Персистенція = DataStore/SharedPreferences; вхід — gear-іконка. Частина лишається dev-tuning, частина може стати справжньою user-персоналізацією. **(a)+(c) можна почати ASAP; (b) — далі.** **✅ (d) НАБОРИ ІКОНОК ЗБУДОВАНО 2026-07-20** (запит Дениса — порівняти варіанти на пристрої й лишити оптимальний): Material Symbols / Phosphor / Tabler, ліцензії звірені за першоджерелом (MIT / Apache-2.0), фіксовано в `THIRD-PARTY-ICONS.md`. ⚠️ Іконки живуть у ДВОХ місцях — Compose і **SDF-образи на мапі**; перемикання оновлює обидва без перезапуску (`applyIconSet`). Прибирається одним блоком (чек-лист в `IconSet.kt`). Там само dev-перемикач МОВИ (en/nb/nn через AppCompatDelegate) — теж тимчасовий. **✅ (a)+вхід ЗБУДОВАНО 2026-07-12 (device-verified):** gear-FAB→`SettingsSheet` (4 стилі Kartverket, вкл. приглушений `topo` через `raster-saturation`), swap на льоту (`MapController.applyBasemap`), персист `SettingsStore` (SharedPreferences), CMP-clean VM. Далі: (b) кольори типів (accessibility) + (c) акцент. Деталі → CHANGELOG 2026-07-12. | `08`, D28, D32 |
| P27 | **Умовні / сезонні варіанти місця** (ідея Дениса 2026-07-19, з огляду аналогів) — те саме місце в різних станах = різні «картки» колекції: зима / світанок / дощ / перший сніг. **Множить контент із тієї самої географії** → прямий удар по **P10** (утримання в малому місті: «закрив 100% Ørsta» ≠ кінець, бо зимова Ørsta — новий шар). Для Норвегії з її сезонами природно. **Третій кут retention** разом із P19 (свіжість) + P24 (новизна маршруту). **Дані дешеві:** сезон = дата, світанок/захід = обчислення, погода = MET/yr (уже плануємо для safety-gate). ⚠️ **Гарди:** (1) **безпека** — «лише взимку / на світанку / в дощ» у **природному** шарі = найризикованіші умови → **місто-first і безпечні умови**; природні — лише за safety-gate, і якщо varsom/yr кажуть «небезпечно», челендж **не підсвічується** (`04` §11.3); (2) **без FOMO/expiry** — пропущена зима не втрата («буде наступної»), жодного штрафу (D20). Post-MVP, opt-in шар | `04` §11.2, P10/P19/P24, D20 |
| P28 | **Географічна completion — «всі [клас] у [одиниці]»** (ідея Дениса 2026-07-19: «в кожної комуни є своє лого») — set-completion над географічною одиницею, **без балів** (чиста повнота набору → сумісно з D13); нагорода = **емблема одиниці**. Рахуються лише **eligible** об'єкти (D6). **Одиниці + що маємо ЗАРАЗ:** **(а) Kommune ✅ дані є** — per-kommune manifest (P18), кордони Kartverket адмін (CC-BY); найкраще для **рідкісних класів** («всі церкви Volda», «всі маяки Ørsta»), бо «всі будинки комуни» нереально (22 645). **(б) Tettsted ✅ вже в продакшні** — per-tettsted `total`/`accessible`/`byType` + тег `tettsted_id` + `TettstedIndex` (P20; SSB Tettsteder, ліцензія **NLOD**); «всі будинки цього tettsted» реально для малих поселень. **(в) Острів (øy) 🆕 треба додати** — полігони Kartverket N50 (CC-BY); **той самий PIP + boundary-патерн, що вже відпрацьовано на P20** → дешево; природно завершена, дуже норвезька одиниця. **Острів = НАСТУПНИЙ крок** (найдешевший × найцінніший). *Черга далі, за умовами:* **fylke** (Kartverket, тривіально — вмикає рідкісні класи «всі stavkyrkjer фюльке») · **grunnkrets** (SSB — лише коли підемо у великі міста; для Volda/Ørsta tettsted достатньо) · **bydel** (лише нац. масштаб — у Volda/Ørsta їх нема) · **verneområde / friluftsområde** (Miljødirektoratet) і **turrute** (Turrutebasen) — разом із природним шаром, за safety-gate · **неформальні регіони** (Sunnmøre / берег фіорду) — **лише через набір комун**, інакше знаменник неверифіковний. ⚠️ **Гарди:** (1) **знімок знаменника** — фіксувати N на момент закриття й **ніколи не відбирати** зароблене (дух D30); нові об'єкти = бонус, не пониження; (2) ⛔ **kommunevåpen — КРИМІНАЛЬНО захищені (перевірено 2026-07-19 по першоджерелах):** straffeloven (2005) **§ 165 b** карає (штраф/до 6 міс.) використання офіційного герба **«або чогось, що з ним легко сплутати»**; Паризька конвенція **6ter** покриває «any imitation from a heraldic point of view». ⚠️ **Стилізація НЕ рятує, а погіршує:** офіційно ухвалюється **blazon** (словесний опис), не малюнок → власний малюнок за описом **І Є** гербом комуни; перемальовування знімає лише авторське право на файл. Порогу «достатньої стилізації» не існує. Дозвіл із 2018 дає **тільки kommunestyret**, у кожному окремому випадку, письмово → реалістично для Volda/Ørsta як партнерів, нереально для 357. ⚠️ **Контур комуни теж відпав:** API `kommuneinfo/omrade` віддає адмінполігон РАЗОМ ІЗ МОРЕМ (Giske 96% вода), 48% комун MR — плями ≤19 вершин на 48 px. **ЩО ПРАЦЮЄ: композитний бейдж** (назва типографікою + контур як фактура + гліф закритого класу) — законно, масштабується, деградує мʼяко; **острів підняти в черзі** — єдина одиниця, де контур працює начисто (острови Giske 32-58 вершин проти 11 у комуни). Фото Wikidata відкинуто (15 ліцензій, 83% з атрибуцією, більшість share-alike). Фолбек — дозвіл комуни; природний додатковий ask у `docs/partner-outreach.md` (поряд із FKB/спонсорством); фолбек — власна стилізована емблема (назва + силует ландмарка). Post-MVP, opt-in шар | `04` §4/§11.2, P18/P20, D6/D13/D30, partner-outreach |
| P29 | **Цифрова turpostkasse (гостьова книга місця)** (ідея Дениса 2026-07-19; аналоги — норвезька традиція toppbok/turbok, YAMAP «люди, з якими розминувся») — у місці коротка гостьова книга: хто був, коли, короткий запис; повертаєшся подивитись, хто прийшов після тебе. Культурно найнорвезькіша ідея набору. **Кандидат на ЗАМІНУ лідербордам** (`01` §7): дає присутність інших **без змагання** → точніше під наш «спокій». **Поетапно:** (1) **особиста локальна turbok — уже покрита D27** (нотатки/фото device-local, без backend) → доступна раніше; (2) **публічна/спільна — лише v2**: backend + модерація + **DPIA**, pseudonymous, opt-in, огрублений час (клас ризику Strava-heatmap, `07`). ⚠️ Non-goal-гард: це **не** «офіційна реєстрація вершин/turmål» — той домен лишається DNT/Statskog (`01` §8). v2 | `01` §7/§8, D27, `07` |
| P30 | **Екран статистики / прогресу** (ідея Дениса 2026-07-19) — окремий екран зі зрізами «скільки розкрито» по **географії × типу**. **Дані вже є** (kommune-manifest P18 + per-tettsted P20 + `byType` + `accessible`) → це **презентаційна задача, не дата-задача**; реально як **fast-follow / MVP-1** (`09` §4). Прямо втілює **принцип самопояснювальності** (`08` §1): плашка каже «6,2% of Volda», а екран відповідає «що таке Volda / а по типах / а по комуні» за ≤2 тапи. Природний дім для **P28** (географічна completion — «7 з 8 церков Volda») і сусід **P26** (Settings — той самий патерн входу). ⚠️ **Гарди:** **прогресивне розкриття** — дефолт ОДИН зріз (поточний tettsted), решта на тап (не вивалювати матрицю — анти-перевантаження D28); **м'який індикатор**, не completionist-бар (`08` §4); **чесний знаменник** — лише eligible + показати, що саме рахується (D6); мова «**розкрито**» (D6); **жодного зведеного score** (D13). **✅ ЗБУДОВАНО 2026-07-19 (device-verified на Pixel).** Форма — **bottom-sheet** (патерн P26; рішення Дениса: шторка, не окремий екран, бо це поглиблення контексту мапи, а не окремий розділ). Зміст v1 — **Coverage + Variety** («N з 6 типів»); **Discovery відкладено** свідомо: будь-яке вікно часу («сьогодні/цього тижня») тихо створює streak-логіку, яку забороняє D20 → окрема розмова. Зрізи: **поточний tettsted (дефолт) ↔ комуна (на тап)**; поза містом показуємо **комунний зріз**, а не порожнечу (закриває 55% сільської забудови, що не мають `tettsted_id`). ⚠️ **Виявилось НЕ суто презентаційною задачею:** чисельників per-unit-per-type не існувало, а per-type `accessible` пайплайн писав, але Android **відкидав** — тому додано `RevealCounters` (чисельники «одиниця × тип» **із Room**), **Room-міграцію 2→3** (`visits.tettstedId`+`kommune`, backfill заднім числом), і **нову географічну одиницю — комуну** (див. нижче). Адверсивна перевірка зловила 9 дефектів чесності чисел (Coverage до **300%**; чипи «X з Y», де X > Y; Variety «2 of 1 types»; «0,0% of Volda+Ørsta» на тайлах без тега; неклампнутий текст; однакові підписи чипів; комуна невідома при холодному старті; 40dp < 48dp; змішані accessible/total знаменники) — усі виправлені під тест. **Device-verified:** Volda tettsted «190 of 3606 · 5,3%» і комуна «190 of 9180 · 2,1%», де сума типів і сума знаменників сходяться точно; 249 розкриттів пережили міграцію (D30). **Тюнінг за польовим користуванням (2026-07-27, device-verified):** порядок вкладок Tettsted/Kommune **фіксований** (був «активний першим» → вкладки мінялись місцями від кожного тапу; позиція чипа — ідентичність, обраність показує заливка) · блок «Samlinger» показує колекції **лише обраної категорії** (презентаційний фільтр; зарахування колекцій його не бачить — D30; заразом стеля `MAX_FEATURED` стала поключовою за типом одиниці, бо спільна дозволяла комунним витіснити всі поселенські) · **окремі гліфи одиниць** `ic_scope_tettsted`/`ic_scope_kommune` **поза `IconSet`** (набори — про гліфи ТИПІВ на мапі, а одиниці на мапі не малюються). Знято з черги: «набори іконок для будівель» — іконки будівель уже в `IconSet` і вже під перемикачем; на мапі ж гліфів будівель нема взагалі (`FillLayer`), тобто це була б **нова фіча**, а не полагодження. Деталі → CHANGELOG 2026-07-19 і 2026-07-27 | `04` §3/§10, `08` §1/§4, P18/P20/P26/P28, D6/D13/D28 |
| P31 | **Тег `kommune` як географічна одиниця** (побічний, але самостійний результат P30, 2026-07-19) — комуни не було НІДЕ в даних (ні на будівлі, ні в manifest `region:"Volda+Ørsta"`, ні на tettsted), а вивести її через tettsted **неможливо**: 55% будівель (12 457 із 22 645) не мають `tettsted_id`. **Канон — `build_tiles.py --kommuner=1577:Volda,1520:Ørsta`**: комуна береться з **індексу пари** вхідних файлів (вхід і так per-kommune) — без PIP і нових джерел; зворотна сумісність перевірена байт-у-байт. **Разовий міст — `retag_kommune.py`** (вхідні GML зникли, Elveg вимагає order-API з поштою): перетеговує НАЯВНІ продакшн-тайли через PIP по офіційних межах Kartverket `ws.geonorge.no/kommuneinfo` (відкритий GET без ключа), зберігаючи **точну парність** (ті самі 22 645/19 723, Elveg-`accessible`, `tettsted_id`, `building_id`) — доведено побайтово. Вихід: `kommune` на будівлі, **`byKommune`** у manifest (total/accessible/byType), `kommune`+`kommune_name` на поселеннях. Незалежна перевірка PIP по ВСІХ будівлях: 0 розбіжностей. ⚠️ При наступному ПОВНОМУ регені числа по комунах можуть зрушити на одиниці (per-pair проти post-hoc PIP) — прикордонна смуга; задокументовано в README пайплайну. Відкриває **P28** («всі 25 сакральних Volda») і дає зміст стану «поза містом». **✅ ВИКОРИСТАНО В UI 2026-07-20:** плашка показує **комунний %** там, де раніше було порожнє «поза містом» (рішення Дениса «а»); підстава з даних MR — **53% забудови поза поселеннями**, а Smøla має 0 будівель у tettsted із 4462. ⚠️ Поселення й комуна часто ЗВУТЬСЯ ОДНАКОВО → комунний рядок мусить явно казати «… kommune», інакше вихід із міста дає «5,3% of Volda» → «2,1% of Volda». Пріоритет гілок: tettsted → комуна → регіон → «поза містом» → локальний fallback (останній лише офлайн: він міряє випадкову величину — скільки тайлів встигло приїхати). **+ сирий `bygningstype` у тайлах** (рішення Дениса: збирати найдетальніше, показувати агреговано) — 116 кодів на MR, довідник зі SSB KLASS; ⚠️ виявилось, що «other» — це проблема ПОКРИТТЯ МАТЧИНГУ (87% без коду взагалі), а не групування | `06`, P28/P30, D6/D31 |

| P32 | **Прогортана мапа не показує вже розкрите — «прогрес зник», якого немає** (знайдено 2026-07-29 у полі, при перевірці переносу даних на `app.streif`) → **✅ ВИРІШЕНО 2026-07-31: варіант «а» — камера читає ТІЛЬКИ локальний кеш** (`8167983`). Зони вантажаться **виключно навколо GPS**: `ensureArea` кличеться з трьох місць (старт застосунку · видача дозволу · зміна версії тайлів), і всі три беруть координати з фікса — обробника камери не існувало. Тож коли людина прогортає мапу до міста, де гуляла вчора, `BuildingStore` тих зон не має, а зафарбування живе на **окремому шарі**, який годується саме з нього → все сіре при цілих даних. ⚠️ **Чому це читається саме як втрата прогресу, а не як «дані ще не приїхали»:** сіра база йде з `buildings.pmtiles` (asset у `style.json`, покриває Volda) і малюється навіть там, де не завантажено жодної зони — тобто видно «наші» будинки, які просто ніколи не фарбуються. Денис зробив рівно цей висновок; на тесті його зробив би кожен, хто гляне на минулотижневий маршрут.<br>**Що вирішило вибір — факт із коду, а не з опису:** `AreaCache` тримає тайли на диску **назавжди** (`filesDir/areas/v{N}/`), а `ensureArea` читає кеш **першим** і йде в мережу лише при промаху. Отже **скрізь, де людина вже гуляла, геометрія вже локальна** — щоб показати там колір, мережа не потрібна взагалі. І не допомогла б: шар фарбує лише те, що є в Room, тож у зоні, де тебе не було, розкривати нічого — з мережею й без неї вона виглядає однаково. На пристрої Дениса цей кеш — 85 тайлів на 7,8 МБ.<br>**Реалізація:** `ensureArea(lat, lon, allowNetwork = false)` з `addOnCameraIdleListener` (гард `CAM_LOAD_MIN_ZOOM`, стеля 24 зони, обхід **в індексах** сітки `AreaCache`). У debug мережа лишається дозволеною — той самий інструмент розробника, яким перевіряють відновлені розкриття, не їдучи в те місце. Три пастки закрито тестами (кожну перевірено **мутацією** — без гарда тести падають): промах кеша не позначає зону завантаженою (інакше **погляд** на квартал ламав би реальне розкривання при **проході** ним), не пише `failedUntil`, і не чіпає `status` (інакше гортання блимало б «loading map data…» і брехало б «no map data here (offline?)»).<br>⚠️ **Чого варіант «а» НЕ лікує (свідомо):** **порожній кеш** — новий телефон після відновлення з Drive (копія несе агрегати Room, не тайли; `allowBackup="false"` не відновлює `files/`) або зміна `DataVersion` (тека кешу міняється, стара підмітається). Саме в цьому стані баг і спіймано 2026-07-29. Відповідь на нього — **варіант «в»**, коли робитимемо переїзд на новий телефон: дотягувати зони **за одиницями з Room** (`tettsted`/`kommune` там уже є), тобто мережею керує «де ти **був**», а не «на що дивишся».<br>⛔ **Альтернативу «малювати розкрите з Room точками, без геометрії зони» ЗАКРЕСЛЕНО**, а не відкладено: у `VisitEntity` координат немає **за задумом** (D14), і це стережуть тести (`BackupMergeTest.model_hasNoCoordinateFields` + whitelist ключів дроту). Щоб малювати без тайла, довелося б покласти координати в Room — зламати рівно ту гарантію, на якій тримається вся декларація приватності.<br>⚠️ **Виправлення передумови (2026-07-31).** У першій редакції тут стояло, що завантаження під камерою — «**новий вид** мережевих запитів: координати того, на що дивишся, а не де ти». **Це неточно.** Підложка в `style.json` — **raster** `{z}/{y}/{x}` з `cache.kartverket.no`, а raster-джерело MapLibre запитує тайли для **поточного вікна перегляду**, не для GPS. Тобто координати того, на що дивишся, покидають пристрій **уже зараз, щоразу як гортаєш мапу**, і саме на цьому побудовано `play-data-safety` §2.2 («підложка на z18 ≈ 150 м заводить нас у precise»). Підтверджує це й сам баг-репорт: сірі будинки було **видно**, отже підложку для того вікна вже завантажено. Новим у варіанті «б» був би не клас запиту, а **другий адресат** (R2 замість лише Kartverket) і геометрія будинків замість картинки. Для обраного «а» це нічого не міняє — мережі там нуль, — але наступне рішення не має спиратись на хибну підставу | `05`, D24/D22, `play-data-safety` §2.1–2.2 |
| P33 | **Ціль нагороди невидима — суперечність між нагородою й механікою розкривання** (помітив Денис 2026-08-01) → 🔴 **РІШЕННЯ НЕ УХВАЛЕНЕ, не вважати вирішеним.** Поки будинок сірий, нічого на мапі не каже про його тип, тож «зібрати всі церкви» цілеспрямовано **неможливо** — лише навмання. Це не дефект реалізації, а суперечність у задумі: **нагороди, які можна переслідувати, вимагають видимості цілі, а механіка розкривання тримається на тому, що ціль невидима.** Вона була там від початку й спливла аж коли нагороди стали конкретними.<br>**Три виходи, кожен із ціною:** **(а)** показувати тип і на сірих будинках — вбиває нагороду за розкриття: якщо церква видно сірою церквою, колір при розкритті вже нічого не відкриває, тобто головна механіка платить за додаткову; **(б)** режим полювання **з самої нагороди** (тап → підсвітити ті об'єкти класу, яких бракує) — opt-in, тимчасово, лише цей клас; ціна: мапа на цей час стає квест-трекером, а це записано в `09` §11 як анти-патерн жанру; **(в)** нагороди не переслідують, їх **помічають** — чесно щодо D20 і спокою, але тоді іменовані сети («Pastor») працюють лише як приємна несподіванка, не як ціль.<br>⚠️ **Ухвалити ДО того, як малювати іконки нагород (G3):** від цього залежить, чи потрібен нагороді екран цілей узагалі. Агент схиляється до (б) зі суворим opt-in, але рішення міняє характер продукту | `09` §11, D20/D28, `04` §13, G3 |

| P34 | **Палітра під 12 категорій** (відкрито 2026-08-02) → **✅ ВИРІШЕНО 2026-08-05: варіант A (12 + обведення), плюс трав'яний `Bolig` #3B6A07 — див. D43.** Порахована палітра (`08` §15.10) провалила перевірку на дальтонізм: за двома незалежними моделями (Viénot/Brettel 1999, Machado 2009) мінімальна ΔE падає з **17,13 до 1,86** — `Handel/kontor ↔ Sakral` = 3,1 (магазин і церква стають одним синім), `Samfunn ↔ Andre` = 1,9 (культура зливається зі смітником). Плюс 5 із 12 кольорів не тримають WCAG 1.4.11 3:1 (uu-tilsynet — юридична вимога). Оскільки візерунки провалились саме на домінантних категоріях (§15.8), **колір лишається єдиним носієм типу**. **Виміряно головне:** обмежує не число 12, а **комбінація «12 кольорів + контраст ≥3:1 до світлої підложки»** — без контрастної вимоги 12 дають worst-CVD 12,7-12,9, з нею стеля 9,1-9,5. Три шляхи: **(A)** 12 + обведення (межу несе stroke → поріг падає; normal 17,8 · deutan 10,6 · protan 10,5) · **(B)** 10 кольорів без обведення · **(C)** прийняти провал. `versionCode 10+` дає перемикач у Settings — рішення ухвалюється **на пристрої**, зокрема на кольоровому `toporaster`. ~~Блокує рівень «б» і 36 іконок~~ — розблоковано. | `08` §15.11.2/§15.11.3, D28 |
> *P9, P11, P12, P14, P15 → перенесені в «Ухвалені» як D26, D27, D28, D29, D30 (2026-06-27). У proposed лишаються **P10** (двигун новизни — прототип+вимір) і **P13** (м'яка гейміфікація) + **P16** (авто-маршрут v2+) + **P17** (bearing-up орієнтація мапи — фаза 5). **Гейміфікація-сесія 2026-07-11 (opt-in шар D33):** +**P21** (Обʼєм-без-формули), **P22** (діорама 2D-first), **P23** (типи-дії), **P24** (новизна маршруту, лінк P6), **P25** (історичний шар). Ідея «3 герої» — відкладено (можливо пізніше), не заведено як P. **2026-07-12:** +**P26** (in-app Settings-екран — стиль карти / кольори типів / акцент). **2026-07-19 (огляд аналогів — YAMASTA / Walksy / YAMAP):** +**P27** (умовні/сезонні варіанти місця), **P28** (географічна completion — kommune/tettsted/острів + емблема), **P29** (цифрова turpostkasse — кандидат на заміну лідербордам). Rarity-класи S–D — **розглянуто, відкладено**; бізнес-штамп-партнерства та «рідкісне в глушині» — **відхилено** (`04` §11.4). +**P30** (екран статистики — зрізи географія × тип; дані вже є → fast-follow/MVP-1).*
