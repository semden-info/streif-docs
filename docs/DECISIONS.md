# Streif — Decisions Log (Журнал рішень / ADR-статуси)

> **Призначення:** єдине канонічне джерело статусу рішень. Кожне рішення має статус: **accepted** (ухвалено), **proposed** (відкрите), **superseded** (замінене). Деталі — у відповідних `01–10`. Створено після Фази-0 gate (стороннє review + адверсивний тех-стек-пас), 2026-06-21.

---

## Ухвалені (accepted)

| # | Рішення | Деталі |
|---|---|---|
| D1 | **Механіка** — зафарбовування на **повністю видимій** карті (НЕ fog-of-war) | `01` §4 |
| D2 | **Реframe** — «персональна мапа новизни», що спонукає до detour; будинки = перша візуальна одиниця відкриття, не кінцева сутність. **Центральна метрика успіху — чи людина справді обрала новий маршрут** | `01` §1/§9 |
| D3 | **MVP-шар — місто-first** | `09` §1 |
| D4 | **MVP — device-local**, два кроки MVP-0 / MVP-1 | `09` §2 |
| D5 | **Матчинг — радіус (MVP), уточнений:** буфер **сегмента** треку (не точки) · враховувати GPS-accuracy · мінімальне реальне переміщення · Activity Recognition як **підказка**, не єдиний gate · vehicle-фільтр із **гістерезисом** (без жорсткого порогу 3–4 м/с). Street-corridor → пізніше | `05` §5 |
| D6 | **Eligibility (B)** — мова «**розкрито/побачено**» (не «відвідано») · без «100%»-completionism · дешевий **build-time фільтр** (будинок рахується лише якщо поруч публічна піша мережа) · повний frontage → fast-follow. **Лок (2026-07-04):** джерело пішої мережі = **OSM `highway`** (footway/pedestrian/residential/living_street/service/path/steps; turrutebasen — стежки природного шару, для міста не те; Elveg — запасний, слабший з тротуарами); **буфер ~25–30 м** (будинок eligible якщо контур ≤буфера від пішохідної дороги; велике шосе motorway/trunk саме по собі НЕ зараховує як пішохідну); прапорець `eligible` рахувати **на сервері** в CC-BY-пайплайні (не на телефоні). Тонке «мій бік vs через дорогу» → **fast-follow** після MVP-0. Буфер польово-тюнабельний (як R_FAR). ⚠️ Ліцензія: OSM=ODbL → використати лише для build-time розрахунку булевого `eligible` (не публікуючи OSM-геометрію); підтвердити «produced work» / розглянути Elveg при міграції (P4). **Реалізовано (dogfood-клієнт) 2026-07-04:** `AreaSource` тягне highway → прапорець `accessible` (buffer 28м, grid), гейт у `matchAt`. Verified (field+пристрій): **112/112 should-розкриттів збережено (нуль хибних негативів)**, відсіяно ~8% недосяжних; ⚠️ across-road НЕ рятує (5/5 should-not лишились accessible → «мій бік vs протилежний» = fast-follow). **Проста side-aware спроба (crossing-check GPS→центр) 2026-07-04 ВІДКОЧЕНА** — ламала 45% правильних розкриттів у щільному місті (residential-дороги всюди) і не ловила target (хибні reveals = мікс across-residential/«зарано», не чиста геометрія). Потрібен «той самий бік/тротуар», не сирий crossing, + більше міток | `09` §3, P4 |
| D7 | **Рендер — двошаровий** (сіра PMTiles-база + visited GeoJSON-overlay), гартований: `withSynchronousUpdate(false)`/**async** (sync ≈2× латентність + колапс fps при пан+апдейті — спростовано на spike, D22) · overlay тільки visited + viewport-scoped · стабільний `building_id` · pin свіжого стабільного MapLibre · **native MapView у Compose** (не wrapper). Feature-state Android — **немає в 13.x** (#1698 open, PR #4219 unmerged) → двошаровий єдиний (D22) | `05` §3.1 |
| D8 | **⚠️ УТОЧНЕНО D31 (2026-07-04): INSPIRE не покриває наш регіон (Sunnmøre, verified) → продакшн-геометрія = OSM self-host (ODbL) + Matrikkelen-збагачення; INSPIRE/FKB — коли з'явиться покриття/публічний партнер.** Первісний план (superseded D31): геометрія з INSPIRE Core2d + тип з Matrikkelen, ship CC-BY. **Чинне (D31): геометрія OSM (ODbL) + тип/номер Matrikkelen (CC-BY) → комбінований ODbL-тайл**; INSPIRE/FKB — апгрейд потім (покриття/партнер, P4). **Spike (наживо 2026-06-21):** INSPIRE WFS зараз віддає лише **точки, не полігони** (підтверджує README `building2osm`) → spike-дані з **OSM** (Volda, 10.8k, `spike/buildings.pmtiles`); чистий Geonorge-CC-BY — продакшн-задача | `06` §3, `11` |
| D9 | **База — Kartverket WMTS `topograatone`** (raster); офлайн через **pre-cache регіону** (OfflineManager) або bundle; ізолювати за інтерфейсом; vector — пізніше | `05` §3.2 |
| D10 | **Tiles — PMTiles**: bundled у assets → копія в `filesDir` → `pmtiles://file://` (бо `asset://` крешить, див. D21); CDN пізніше | `05` §7 |
| D11 | **Сховище — Room + BundledSQLiteDriver**. **Реалізовано 2026-07-04 (MVP-0-старт):** `MvpDatabase` (visits+sessions, колонка `source` за D30), KSP `2.2.10-2.0.2`+Room `2.8.4`, importer `visited.txt`→Room (132 dogfood-розкриття мігрували, verified на пристрої), session-checkpoint (лікує D23 #11), DEBUG-дубль у `visited.txt` для `analyze.py`. **⚠️ AGP 9.2.1 built-in Kotlin потребує `android.disallowKotlinSourceSets=false` для KSP.** Spike-шорткати→полір: класичний драйвер (BundledSQLiteDriver пізніше) + `allowMainThreadQueries` (винести на IO-dispatcher) | `05` §3, CHANGELOG |
| D12 | **Backend — НЕ в MVP.** Build-time пайплайн = CLI (`building2osm`/GDAL → Tippecanoe → PMTiles), без БД. Laravel/Filament/PostGIS/Sanctum/Redis/FCM — лише v2, обирати після sync/social-вимог | `05` §8 |
| D13 | **Метрики — три виміри: Coverage / Variety / Discovery.** Без єдиного score/XP, без формул балів. «Discovery» у MVP = особиста новизна; rarity з cap (×2, публічні об'єкти, opt-in агрегати) — пізніше. **⚠️ Уточнення (2026-07-11, D33):** керує **дефолтним/спокійним** досвідом; opt-in шар гейміфікації (post-MVP, D33) може нести багатші механіки, але rarity/difficulty **НІКОЛИ не множник нагороди** (Merkehåndboka — лише описова/safety-мітка), а стеля рідкісності = ця сама capped(×2) opt-in community-rarity; безпека тримається крос-режимно | `04`, D33 |
| D14 | **Приватність — device-local мінімізація; сирий GPS ніколи на сервер.** Diagnostic mode тримає сирий трек **локально ~30 днів** (ручне видалення; у релізі — налаштування) | `07` §4/§6 |
| D15 | **Offline-first (уточнено)** — tiles будинків bundled; база pre-cached/bundled для регіону; **не** live WMTS у полі | `01` §7, `05` §3.2 |
| D16 | **Регіон MVP — Volda + Ørsta**; перший зріз spike — **Volda** (kommune 1577) | `09` §7, `11` |
| D17 | **Spike — розділений:** Spike 1 (реальні дані → PMTiles → **симульований GPS** → кольорування), Spike 2 (реальний FGS + diagnostic) | `10` §4 |
| D18 | **Локалізація** — bokmål + nynorsk + англ.; default за пристроєм (fallback NO→EN). **Каркас реалізовано 2026-07-04:** `values/`(en)+`values-nb`+`values-nn`, UI+нотифікація; норв. переклади чернеткові (звірити носієм); діагностичні нотатки gate/loading — follow-up | `08` §11, CHANGELOG |
| D19 | **Фреймворк** — native Kotlin + Compose; логіку тримати в чистих модулях для дешевого KMP-iOS пізніше | `05` §3 |
| D20 | **Дух гейміфікації** — спокій, без streak, святкувати віхи без тиску, тижневий підсумок (in-app, без push) | `04` |
| D21 | **Spike-1 (рендер) ✅ підтверджено на Pixel 9 (2026-06-21):** MapLibre Native рендерить ~10.8k кольорових будинків Volda. Знахідки: (а) **PMTiles вантажити `pmtiles://file://`** (копія asset→filesDir на 1-му запуску); **`pmtiles://asset://` крешить** `incorrect header check` (баг AssetFileSource, усі версії 12.3.1–13.3.0); (б) `.pmtiles`-asset потребує **`noCompress`**; (в) тайли — **tippecanoe** (не GDAL); (г) рендер так само ок із GeoJSON-джерелом (запасний шлях) | `05` §3.1, `11` |
| D22 | **Spike-1 v2 (механіка+перф) ✅ provisional на Pixel 9 (2026-06-22):** двошаровий overlay (сіра PMTiles-база + visited `GeoJsonSource`, колір за типом) тримає **~60 fps пану до 4000 visited** у viewport; **async** `setGeoJson`-латентність ≤~100 мс до 2000 (148 мс@4000); пам'ять стабільна (GC-sawtooth, без витоку, ~41 МБ@1387). Радіус-grid матчинг точний (1387/1387 по маршруту). **sync — погано:** ≈2× латентність + **колапс fps при пан+апдейті** (N=1000/2000/4000 → 48/19/5 fps проти стабільних ~60 async). Інструмент — **власний MapLibre frame-listener** (HWUI-tools SurfaceView не бачать). Gate **відкритий до mid-range** (Pixel 9 — флагман) | `05` §3.1, `11` §2–5 |
| D23 | **Незалежне review — наступне на межі Spike 2 → MVP-0** (повний стан проєкту + tech-stack-optimality re-check; spike→продукт = остання дешева мить виправити фундамент). Далі вужче: **data/licensing** при CC-BY-пайплайні (P4), **повне+security** перед v2 (backend/social). Формат: адверсивний review свіжими агентами (без вкладення в попередні рішення) + опц. людина-рецензент | `10`, P4/P5 |
| D23.1 | **Review ВИКОНАНО (2026-07-01) — вердикт «готово до MVP-0 з умовами».** Дві незалежні гілки (10-агентний адверсивний воркфлоу Opus: 18 знахідок→16 підтв./2 спрост.; + codex gpt-5.4), знахідки верифіковано по коду. **Архітектура здорова — переробки не треба** (MapLibre Native, двошаровий overlay, `AreaSource`-шов, flat-файли-на-тюнінг). **3 реліз-блокери:** (1) `allowBackup=true`→`filesDir` (у debug — сирий diag.csv) у Google-хмару = порушення D14; (2) Overpass ODbL→CC-BY CDN; (3) `building_id` не переживе зміну джерела (D30). **Кластер matching/cold-start**, що спотворює польові дані: тангенційний промах (корінь Ristevegen 1), буфер replay 12≈24с < часу fetch, `ensureArea` під dwell-гейтом. **Застосовано 3 дешеві фікси** (allowBackup=false; ensureArea з-під gate; reattach store). Повний список і перебіг → CHANGELOG 2026-07-01 | CHANGELOG, D30/P2 |
| D24 | **On-demand area download** (заміна фіксованого бандлу — польовий тест 2026-06-26 довів, що bbox не масштабуються під рух користувача: розкривалось лише там, де вгадано зону). `BuildingStore` інкрементальний + `AreaLoader` тягне будинки в bbox навколо GPS на льоту + локальний кеш; **джерело за інтерфейсом**. **Тест = Runtime Overpass** (ODbL, нуль інфри, флакі — ок для dogfood, зону тягнемо раз і кешуємо). **⚠️ ОБОВ'ЯЗКОВО мігрувати на Pre-hosted CC-BY CDN ПЕРЕД публічним релізом** (надійність + масштаб для багатьох юзерів + ліцензія) — конкретний тригер для **P4 / D8 / D10**. Запит Дениса 2026-06-26: не забути цей перехід | `05` §7, P4 |
| D25 | **Матчинг — edge-distance + closest-approach** (заміна центроїд-within-R; польовий присуд 2026-06-26: 2147 фіксів + 91 ручна мітка + adversarial-воркфлоу). Корінь ОБОХ скарг («зарано» + «пропуск через дорогу») — міряли відстань до **центроїда**, не до **контуру**. Зміни: (1) GPS→**контур полігона** (point-to-ring, PIP→0), локальна проєкція за широтою запиту; (2) **bbox-grid** — будинок у всі комірки, що накриває його bbox (великий/видовжений із далеким центроїдом не випадає); (3) **R_FAR=18 м**; (4) **closest-approach gate** — розкрити коли edge виросла на **SLACK=1.5 м** над мінімумом (проминув найближчу точку) АБО стіна ≤ **MIN_EDGE=8 м** (впритул-байпас). **closest-approach, НЕ abreast:** adversarial-рев'ю показало курс на 2-м базі шумний (P(похибка>75°)=36%) → abreast крихкий; closest-approach course-free, помиляється «пізніше». **Replay на тому ж треку:** recall 69→76/83, edge@reveal 19.2→9.9 м, видовжені «пройшов за 2-6 м» тепер ловляться. `R_FAR`/`MIN_EDGE`/`SLACK` легко-конфігуровані для польового тюнінгу (P2). НЕ чіпати: vehicle-gate, ACC_MAX=30, building_id | `05` §5, P2 |
| D25.1 | **Тайминг матчингу — closest-approach до ЦЕНТРОЇДА** (рефайн D25, польовий ретест 2026-06-27: 1563 фікси + 66 міток). Скарга «розкривається до того, як дійшов» лишалась, бо `MIN_EDGE=8` спрацьовував на **передньому куті** будинку. Денис: ідеал — розкривати «**приблизно по середині, коли йдеш уздовж**». Зміни: **eligibility** лишається edge ≤ R_FAR=18 (через дорогу/видовжені), а **тайминг** тепер по closest-approach до **ЦЕНТРОЇДА** (`runMin` центроїд-дист; розкрити коли виросла на **SLACK_CEN=2 м** над мінімумом = проминув середину) + малий байпас **MIN_EDGE_NEAR=3 м**/всередині (впритул) замість MIN_EDGE=8 + **`moved`-guard** (тайминг-розкриття лише на реальному русі ≥MIN_MOVE — проти GPS-джитера на місці). **Replay на тому ж треку:** розкриття лягає медіана **3.3 м від центру** (79% у межах 5 м) проти переднього кута; ціна — recall 61→56/61 (будинки, повз середину яких не пройшов). `SLACK_CEN`/`MIN_EDGE_NEAR` — тунабельні (P2) | `05` §5, P2 |
| D26 | **Deferred auth — пропонувати, не вимагати** (перенесено з P9, 2026-06-27) — логін не на старті; пропонувати акаунт/збереження прогресу **після** розкриття частини карти (момент цінності). MVP-0 device-local без логіну (D4); формалізує onboarding + шлях до опц. cloud-backup (v2) | `08` §10, D4/D14 |
| D27 | **POI «цікаві місця» + фото/нотатки — у природному шарі** (перенесено з P11, 2026-06-27) — (1) read-only курований POI з відкритих даних (Stadnamn+lore, turrutebasen, DNT, Wikidata) без backend; (2) особисті локальні нотатки/фото device-local (D14); (3) публічні фото/відгуки — лише v2 (backend+модерація+DPIA). Увесь блок — природний шар, НЕ міська версія | `06`, `07`, D12/D14 |
| D28 | **Візуальна мова — мінімалізм, без перевантаження** (перенесено з P12, 2026-06-27) — прості контурні іконки колір-за-типом (приклад Walksy), низька щільність маркерів (анти-StreetComplete), ≥48dp + читабельні написи, дорослий тон. Collect/Unlock = приємне «розкрито» + колекція, **без абстрактного score** (лічильники повноти — ок; м'яка гейміфікація — P13) | `08` §1/§8/§14, D13/D20 |
| D29 | **UX точності GPS — не розкривати/не пропускати тихо** (перенесено з P14, 2026-06-27) — показувати стан надійності GPS, не мовчазний мис-reveal/пропуск (урок Turf); доповнює edge-матчинг | `05` §5, D25/P2 |
| D30 | **Захист від ретроактивної зміни «розкрито»** (перенесено з P15, 2026-06-27) — апдейти даних Kartverket не «знімають» уже розкрите (анти CityStrides/OSM); стабільний `building_id` (D7/D22) + інваріант «розкрите лишається розкритим». **⚠️ D23-review (2026-07-01): поточний `building_id="w"+wayId` (OSM) НЕ переживе Overpass→CC-BY (INSPIRE/Matrikkelen — інший id-простір) → усі персистовані розкриття осиротіють, мапа тихо скинеться. Найвищий незакритий ризик міграції. **УХВАЛЕНО (2026-07-04):** продакшн `building_id` = Matrikkelen `bygningsnummer` (напр. `m<nr>`), приходить у CC-BY-пайплайні (join INSPIRE-геометрія + Matrikkelen тип+номер). Overpass-era dogfood-розкриття = одноразові (clear при зміні джерела) АБО опц. одноразовий centroid→будинок spatial-remap історії Дениса; Room несе колонку `source` (osm-test/matrikkelen). Геометричний хеш (centroid-geohash) **відхилено** — OSM≠Matrikkelen-контур дає зсув + колізії сусідніх дрібних будівель. Додати SOURCE_VERSION у ключ кешу (інакше старі Overpass-тайли завантажаться як CC-BY).** | `05`, D7/P4, D23.1 |
| D31 | **CC-BY продакшн-дані — рішення пайплайну (лок 2026-07-04; конкретизує D8/D24/D6/D30/P4).** (1) **Eligibility-джерело продакшн (server-side D6) = Kartverket Elveg** (дороги; чиста CC-BY-сумісна ліцензія — на відміну від OSM ODbL; OSM-highway лишається ЛИШЕ dogfood-клієнт). (2) **Хостинг = безкоштовний статичний CDN** (Cloudflare Pages/R2 або аналог; некомерційний соло-проєкт). (3) **Обсяг = регіон-first** (Volda/Ørsta → Møre og Romsdal), пайплайн масштабований на всю Норвегію. (4) **Міграція Overpass-era розкриттів = одноразово стерти** (dogfood-тестові; spatial-remap лише якщо зберігати історію). Формат = per-tile GeoJSON на сітці 0.02°; id = Matrikkelen bygningsnummer (D30). **Крок 0 (data-спайк, ВИКОНАНО 2026-07-04):** прямі Geonorge WFS/API-тести → **INSPIRE Buildings НЕ покриває наш регіон** (Осло-bbox 499 буд., Møre og Romsdal-bbox **0**); точних відкритих CC-BY footprints для Sunnmøre нема (FKB `AccessIsRestricted:true`). **ОБРАНО: Варіант 1 — OSM-геометрія (self-host, ODbL) + Matrikkelen-збагачення** (bygningsnummer+тип через point-in-polygon join; CC-BY). **FKB = апгрейд ПОТІМ через публічного партнера** (Volda/Ørsta kommune, Møre og Romsdal fylkeskommune, Høgskulen i Volda — усі Norge-digitalt-партнери з доступом до FKB; дають дані+легітимність+спонсорство; приватному ENK FKB — платна ліцензія через Geodata/Norkart). **УТОЧНЕНО (2026-07-04, юр-research, першоджерела):** партнер НЕ може просто «дати» FKB для власного застосунку Дениса — субпідряд-договір Norge digitalt (`Standardavtale for utlån … underleverandører`) забороняє (no rights acquired / delete-after-assignment / non-commercial / лише завдання партнера); освітня bruksright HVO не покриває ліцензування FKB у публічний застосунок. Життєздатно ЛИШЕ якщо Streif = проєкт партнера (партнер = data controller, Денис = databehandler під його bruksrett) АБО партнер спонсорує купівлю. **OSM+Matrikkelen лишається основним шляхом — FKB не блокер.** Відкрите юр-питання (постійна utadrettet-послуга vs oppdrag-з-видаленням) → `post@norgedigitalt.no`. Стратегія+готовий лист → `docs/partner-outreach.md`. Комбінований тайл = ODbL (share-alike); атрибуція © OpenStreetMap + © Kartverket. Апгрейд без зміни архітектури (AreaSource-шов, Matrikkelen-номер як id). **РЕАЛІЗОВАНО (2026-07-04):** пайплайн `spike/pipeline/` (Matrikkelen GML + OSM Overpass → point-in-polygon join → D6-accessible → gzip-тайли 0.02°, id=`m<bygningsnummer>`); join Volda 98.5% / Ørsta 84% (стеля — OSM має більше об'єктів ніж Matrikkelen); **Volda+Ørsta = 416 тайлів (8.2→2.2 MB gzip) живі на Cloudflare R2** (`pub-b1c9ae365792405880b62e24ccda0df1.r2.dev`; r2.dev-now → custom-domain `tiles.streif.no` на першого зовнішнього тестера). **Хостинг обрано R2, не Pages** (Pages — стеля 20k файлів, впремося на нац.масштабі); **$0** (лімітує читання 10M/міс — юзаємо ~0.09%; egress безкоштовний; сховище 10 GB); **R2 сам НЕ стискає → тайли передстиснено** (Content-Encoding: gzip, ~57 KB найбільший). Клієнт: `CdnGeoJsonAreaSource` + `BuildConfig.USE_CDN`/`CDN_BASE_URL` (Android-код витримує 3 gzip/404/Content-Length-пастки). Хостинг-план верифіковано адверсивним воркфлоу (R2 vs Pages + 3 скептики: cost/prod/correctness, веб-джерела Cloudflare docs). **Elveg-eligibility ✅ ЗРОБЛЕНО (2026-07-04, Варіант B):** тайли-`accessible` рахуються з **Kartverket NVDB Vegnett Pluss** (= розповсюджуваний Elveg 2.0; UUID `97e6a869-…`, CC BY 4.0, GML, EPSG:5973≈25833; walkable-фільтр по `typeVeg` + `vegkategori`) **+ OSM-footpath-bridge** (A/B-гейт на Volda: Elveg-only регресував 309 буд. — hytte/outbuilding на неформальних `sti`/`traktorveg`, чия міграція в Elveg триває 2026–2027; bridge = нуль регресії, accessible регіону 83→87%). Обрано B бо тайл **і так ODbL** (геометрія OSM) → bridge не додає ліцензійної ціни зараз; прибрати коли геометрія→FKB І Elveg домігрує стежки, або +Turrutebasen. **⚠️ Pipeline-vs-runtime:** offline-CDN-тайли несуть Elveg-eligibility; runtime on-demand (Overpass, USE_CDN=false) на девайсі й далі рахує accessible з OSM-highways — вирівняється коли Elveg-мережа поїде pre-hosted. `parse_elveg` + `--elveg`/`--osm-bridge` у `spike/pipeline/build_tiles.py`; A/B → `elveg_ab.py`. **TODO:** custom-domain перед зовнішнім тестером + вимкнути r2.dev | `06`, D8/D24/D6/D30/P4 |
| D32 | **UI-стек = Jetpack Compose + base Material 3 (лок 2026-07-05; верифіковано адверсивним воркфлоу — 3 research + devil's-advocate + вердикт, веб-джерела 2026).** Розглянуто ВСІ 6 сучасних опцій: Compose+M3 ✅, Material 3 Expressive (à-la-carte), Flutter ❌, React Native ❌, KMP+Compose Multiplatform (не зараз), legacy Views ❌. Адверсивний агент чесно не зміг побудувати реальний кейс проти. **Обрано Compose + base M3** бо: нативний Kotlin (нуль-bridge до FGS/Fused/sensors/battery/Room — вони живуть НИЖЧЕ UI, framework-invariant); MapLibre Native Android v13.1.0 (first-party — обгортки Flutter/RN відстають на версії + Flutter має проблеми з фоновим GPS, ~$500/рік плагін); solo-dev (один skillset); View/XML заморожено (maintenance mode). **Уточнення до плану:** (1) писати Compose **«CMP-clean»** (UI-логіка поза Android-специфічними типами) → двері iOS через KMP+CMP відкриті БЕЗКОШТОВНО, портування замість rewrite; (2) **Material 3 Expressive à-la-carte** — base M3 `1.4.0` як floor, брати вже-graduated Expressive-компоненти з `1.5.0-alpha` вибірково (не чекати stable 1.5.0); (3) **map у Compose через `AndroidView` над наявним MapView** (перевірено), НЕ через pre-1.0 `maplibre-compose` v0.13.0. Візуальна мова = **D28 / `08`**. Відомий framework-invariant ризик: нативний OOM MapLibre при пануванні динамічних polygon-шарів (у SDK, який уже поставляємо) — відстежувати. **Реалізовано (2026-07-05, проміжно на Views до міграції):** emoji-grey-кнопки → Material `FloatingActionButton` (mini) + `MaterialButton` (pill) + векторні Material-іконки; повна Compose-міграція UI = окрема MVP-0-задача (план у `10`) | `05`, `08`, D28 |
| D33 | **Гейміфікація = спокійний кор + опційний шар, що вмикається** (лок 2026-07-11; обговорення Дениса + codex-review, звірено з D13/D20/D2). Замість вибору «спокій vs гейміфікація» — **спокійний кор (дефолт, MVP-1) + opt-in шар гейміфікації (post-MVP)**, «регулятор глибини», не форк; предметне втілення «спокій за замовчуванням, челенджі опційні» (`01` §7). **D13/D20 керують дефолтом.** Спірні механіки брейнсторму (`04-2` → влито в `04`) живуть в opt-in шарі під **крос-режимними інваріантами:** безпека **не буває opt-in** (жодна нагорода не штовхає в ризик); без втрати прогресу при перемиканні (D30); без streak/FOMO навіть у гейміфікованому; спокійний режим = **повний**, не тизер. Реалізація — дешевше через **гранульовані тумблери** (цілі/колекції/челенджі/серпанок), іменований пресет пізніше за попитом. Механіки → P21–P25; MVP-1 везе лише кор | `04`, D13/D20/D30 |
| D34 | **Розкривання об'єктів = 2 рівноправні стовпи (Місто / Природа), механіка за ГЕОМЕТРІЄЮ** (design-сесія Дениса 2026-07-12, метод point-by-point walkthrough; розширює **D27**, зчеплено з P6/P10/P16; Stikk UT — філософський прецедент «усі turmål рівні, без вагового score» = наша D13). Кор Streif лишається **ambient-reveal**; поверх — рівноправна «збирай»-механіка (turmål/POI з ВІДКРИТИХ даних). **8 ухвал:** **①** два стовпи. **②** групування **Місто vs Природа**; ⚠️ альтернатива «Розкривай (площі+лінії) vs Збирай (точки)» — **pivot-кандидат** за даними retention (не викидати). **③** перемикання **гібрид**: авто-кадр за tettsted (реюз P20) + **м'яко** (обидва стовпи завжди рахуються, змінюється лише акцент UI) + **safety консервативно** (пасивна інфо поза містом завжди; повний похідний-режим — явним тапом) + **іконка режиму** (показує авто-режим, тап = ручне перемикання). **④** механіка за геометрією: **площа**=edge-матчинг (D25, є) · **точка**=proximity (нове, дешеве; вершини — тісніший R~25-30 м, без перевірки висоти) · **лінія**=«пройшов уздовж» (нове; **стежки-комбо**: заповнення кроком 10% + позначка «пройдено» при ~75% [поріг тунабельний]; «%»=**покриття**, не метри; **вулиці НЕ фарбуємо** — будинки покривають місто; стежки лише поза містом). **⑤** зарахування точок **гібрид**: авто за близькістю + опційний тап «фото/нотатка» (=D27). **⑥** дані **комбо** (як D31): відкриті ЗАРАЗ (стежки=Kartverket **Turrutebasen**; піки=Stadnamn/OSM; POI=OSM+Kulturminne+Naturbase+Wikidata; хижки=DNT/Nasjonal Turbase) + **Friluftsrådet-партнерство** у roadmap (куровані turmål + легітимність + спонсорство [Sparebanken→StikkUT-прецедент] + FKB). ⚠️ **НЕ інжестити куровані бази Stikk UT/MoroTur** (sui generis database right + ToS) — «є в turmål» як сигнал якості лише через партнерство / відкриті проксі (Wikidata/Kulturminne) / ручну курацію свого регіону. **⑦** курація **гібрид**: алгоритм-фільтр (тип+назва+дедуп) + людина/партнер + **тірування** (куроване яскраво / сире тьмяно = declutter D28). **⑧** черга: обидва режими на **тест ASAP** (safety = **release-gate, НЕ test-gate** — тестують проінструктовані знайомі, `03` §D); порядок за складністю — міські POI → вершини/хижки → стежки+повний safety-gate (varsom/yr/SOS); **курація-адмінка → v2** (Filament, D12). **Дані-готовність verified 2026-07-12:** Turrutebasen (Geonorge WFS uuid `d1422d17`) + OSM Volda/Ørsta (22 hiking-маршрути, 2346 path-ліній, 222 піки, 540 POI) — достатньо почати; прогалини (повнота DNT-хижок, safety-шар) — перед релізом. **✅ POINTS-ONLY ЗБУДОВАНО (інкр. 1-4, 2026-07-12; компіляція+JVM-тести; рендер+картка device-verified; walk-зарахування pending польової прогулянки):** пайплайн `fetch_poi`/`build_poi` (POI з OSM, city/nature-тег, провенанс, 249 POI на R2; `--city-only` тест-allowlist = 11 міських перлин) + Android `PoiType`/`PoiIndex` + SymbolLayer (Walksy-маркери) + proximity-зарахування (Room `poi_visits`, вершини+dwell) + тап-картка (з фото) + іконка режиму. **Фото на картці:** Wikidata-клейм **P18 → Wikimedia Commons** (ілюстративне фото місця, `--images`; 4/11 міських POI: Volda/Ørsta/Hornindal kyrkje + монумент Mahler; ⚠️ **пастка:** Commons віддає **403** на дефолтний User-Agent → `StreifApp` ставить описовий UA через OkHttp-інтерсептор; **pre-release TODO:** per-image кредит автор+ліцензія — Commons=CC-BY-SA, generic «Foto: Wikimedia Commons» недостатньо на релізі, ліцензійний ризик як ODbL/CC-BY по картах). **Стежки (лінія-reveal) — окремий spike далі.** Деталі → CHANGELOG 2026-07-12. Механіки → `04` / `03` §B-D. **REFINED post-codex-review (2026-07-12; незалежний codex-огляд, 14 знахідок, verdict «не готово до повного режиму» — прийнято):** v1-природи **звужено до points-only**: (a) **стежки (лінія-reveal) відкладено** в окремий spike — складність недооцінена (сегментація/GPS-дрейф/персистенція сегментів [D30]/out-and-back/анти-чит), цінність недоведена; (b) стовпи **НЕ рівні в v1** — **reveal головний, collect другорядний** (дорости до рівних за retention-даними); (c) **POI і в місті, і на природі**, але місто — **легко/куратно** (кілька перлин, тірування, без клатеру D28); (d) **іконки типу на мапі + мінімальна калм-кор картка** (фото+назва+бейдж типу+«розкрито»+опц. lore-лінк P25; рідкість/таймери/челенджі → opt-in шар D33, НЕ v1); (e) **лишаємось на повністю видимій мапі + серпанок, НЕ fog-of-war** (D1/safety — реафірмовано); (f) **курація лін** — одне джерело (OSM) + ручний allowlist Volda/Ørsta (не мульти-джерельний data-product одразу); (g) **вершини** +dwell+topo-sanity (тісний-R-без-висоти хибно зараховує дорогу/оглядову); (h) **safety на тесті** — allowlist лише безпечні/доступні місця + мінімальний disclaimer; погоду не інтегруємо (usery мають yr); повний gate (varsom/yr/SOS) — реліз; (i) **провенанс per-feature** (source/license/source_id/last_verified) з дня 1. Деталі огляду → CHANGELOG 2026-07-12. | `04`, `03`, D27/D13/D31/D28/D1/D20, P6/P10/P16 |

## Замінені (superseded)

| # | Було → стало |
|---|---|
| S1 | Laravel + PostGIS як MVP-стек → **D12** (CLI build-пайплайн; backend на v2) |
| S2 | `feature-state` рендеринг (`03` §A) → **D7** (двошаровий overlay) |
| S3 | «FKB/INSPIRE — CC-BY 4.0» (CLAUDE.md) → **D8**, уточнено **D31** (INSPIRE не покриває регіон → продакшн-геометрія = OSM self-host ODbL + Matrikkelen; FKB/INSPIRE — потім через покриття/партнера) |
| S4 | «Немає Android-застосунку для щоденного дослідження» (`01` §2) → **D2** (Fog of World — на Android; CityStrides — веб; Walkable — iOS) |
| S5 | Негайне видалення сирого треку (`07`) → **D14** (локальний diagnostic 30 днів) |
| S6 | «Формули балів / difficulty» (`03` §E) → **D13** (без score; три виміри) |
| S7 | D7 `withSynchronousUpdate(true)` → **async (false)** — sync ≈2× латентність + колапс fps при оновленні під час пану (spike D22) |
| S8 | Фіксований мульти-сіті бандл будинків (стопгап Vinstra/Torpo/Enger/Oslo) → **D24** (on-demand area download; bbox не масштабуються під рух користувача) |
| S9 | Матчинг до **центроїда** будинку (within-R, диск+сегмент, D5) → **D25** (edge-distance до контуру + closest-approach gate; центроїд спричиняв і «зарано», і пропуск через дорогу/видовжених — польовий присуд) |

## Відкриті (proposed)

| # | Питання |
|---|---|
| P1 | **Назва «Streif»** — лишити робочою; до логотипа: Navnesøk (домен + foretaksnavn + trademark) · тест розуміння на ~10 норвежцях · searchability в сторах (колізія з австрійською трасою Streif) |
| P2 | **Фінальні пороги матчингу** — частково вирішено польовим присудом D25 (edge-метод + R_FAR=18/MIN_EDGE=8/SLACK=1.5, ACC_MAX=30). **Лишається тюнінг `R_FAR` (18↔20)** на ширші вулиці — 6/15 пропусків ще за межами 18 м; збирати ще ✓/✗-мітки (маркування лишити ON) і переганяти `analyze.py`. Пороги перенавчені на 1 трасі (Volda) → переперевірити на щільнішій забудові. **⚠️ D23-review (2026-07-01): НЕ тюнити `R_FAR` на поточних даних — recall спотворений двома багами: (а) тангенційний промах (eligibility-вікно `edge≤R_FAR` закривається перш ніж центроїд виросте на SLACK_CEN — будинок випадає з кандидатів → `passedMiddle` не спрацьовує; корінь Ristevegen 1), (б) cold-start: буфер replay 12≈24с < часу Overpass-fetch → ранні фікси витісняються. Спершу розчепити eligibility/timing-вікна (тримати `runMin`-оцінку кілька фіксів після виходу за R_FAR) + розширити replay-буфер за часом/дистанцією, ТОДІ чиста повторна прогулянка. Справжнє розрізнення «вздовж 15м vs через дорогу 15м» — це D6 (піша мережа), не поріг.** **Присуд 2026-07-04 (чистий walk на реальних фіксах):** recall 162/164 (99%) живцем, пропуски ↓3×; пороги на оптимумі — жодна зміна R не покращує (R=16…22 → хибних 7/7); тайминг `SLACK_CEN 2→1` (промптніше за фідбеком, без цінності recall/precision). Лишається ТОЧНІСТЬ (across-road/«зарано») — окремий алгоритм + більше даних, не поріг. «Пізно після проходу» (p90 ~10м) = холодний старт зони → префетч наперед. |
| P3 | **Джерело меж для Coverage-%** — ✅ **вирішено P20** (SSB Tettsteder; grunnkrets дрібніше пізніше). *(Було: bydel/grunnkrets — Volda/Ørsta не мають офіційних bydeler.)* |
| P4 | **Ліцензія footprint INSPIRE + офлайн-копіювання** — письмово підтвердити з Kartverket |
| P5 | **v2 backend** (Laravel vs Supabase vs Django) — обрати коли scoped |
| P6 | **Пройдені вулиці/маршрути як доповнювальний шар** (CityStrides-стиль, але «особиста спокійна мапа», не змагання) — оцінити **після** spike будинків; поки **не додаємо** (2026-06-21) |
| P7 | **feature-state на Android** — стежити за PR #4219 (issue #1698); якщо приземлиться у 13.x/14.x → оцінити **одношаровий** recolor тайлованої бази замість overlay (стабільний `building_id` уже готовий) — D22 |
| P8 | **Zoom-важіль overlay** (ідея Дениса) — `overlay maxzoom` → агрегат на дальньому зумі (+ опц. `minZoom`), виставити під виміряну стелю; на Pixel 9 не обов'язковий (60 fps@4000), але страховка для mid-range; діє на overlay, не на тайловану сіру базу — `08` §9, D22 |
| P10 | **Двигун «нові місця» + утримання в малому місті** (ключова потреба Дениса, 3× у фідбеку) — як спонукати відкривати нове, але щоб юзери малих міст не закинули після «100%». Напрям (не суперечить D1/D13/D20): (а) опц. **свіжість/серпанок** — розкрите бляне без повторного візиту, але **НЕ зменшує lifetime-стат** (відповідь на топ-побажання Fog of World); (б) **Variety/Discovery** (D13) дають цілі поза Coverage-% навіть у малому місті; (в) **природний шар** (стежки/вершини/turmål) = «нескінченний» контент навколо малих міст; (г) сезонні/часові стани + живa мапа (нові дані). Heatmap-інтенсивність — post-MVP (`09` §5). **Прототипувати й МІРЯТИ retention, не приймати аксіомою** | `04`, `01` §9, D2/D13 |
| P13 | **М'яка гейміфікація — відкрите** (Денис ще обмірковує, 2026-06-26) — (а) **повнота колекцій у числовому вигляді** (X/Y типів, X/Y районів) — це спокійний прогрес-лічильник, НЕ абстрактний score → сумісно з D13; (б) ідея **щоденної колекційної нагороди** на випадковій будівлі на пішій відстані, **в межах міста користувача** (простіший варіант — не треба зберігати «часте місце старту»; навіть якби зберігали — це device-local, не порушує D14). ⚠️ Guard: opt-in, **без streak-покарання/expiry** (D20). **Тверда межа (Денис 2026-06-27): НЕ примушувати ходити щодня й НЕ штрафувати за пропуск — будь-яка щоденна механіка лише як необов'язковий подарунок.** Синергія з P10 (нагорода віддалік = стимул до нового). *Імпорт історії GPX/Timeline — знято (не потрібен, рішення Дениса).* | `04`, D13/D20, P10 |
| P16 | **Авто-маршрут до недослідженого (v2+)** — застосунок САМ пропонує маршрут, що максимізує нову територію; користувач задає **довжину (час або відстань)** і **тип (в один бік / кільцевий)** (фіча-запит Дениса #9). Обчислювально важко (Chinese-postman / max-new) → v2+; підсилює reframe «новий маршрут» (D2) | `10` §11, D2 |
| P17 | **Орієнтація мапи «за напрямком руху» (bearing-up)** — фіча-запит Дениса (як автонавігатор). **Фіксовано:** опційний перемикач, **дефолт north-up** (для пішохода course-up дезорієнтує сильніше, ніж в авто — зупинки/розвороти/півоти на місці; Walkable-подібні тримають north-up + напрямлений puck). **Відкрите:** джерело курсу — **компасний heading (куди дивишся, `ROTATION_VECTOR`)** vs **GPS-course (куди йдеш, `Location.bearing`)**; для ходьби компас природніший, але шумніший у приміщенні/біля металу → можливо дати обидва або авто-вибір. **Реалізація:** швидкий спайк у наявній камері (`CameraPosition.bearing` + puck `SymbolLayer.iconRotate`; згладжування обов'язкове — мертва зона <~0.8 м/с, тримати останній курс, кутовий low-pass через 360°) АБО (чисто, MVP-1) MapLibre `LocationComponent` (`RenderMode.COMPASS`/`GPS` + `CameraMode.TRACKING_GPS`) — заразом прибирає ручний me-шар + `easeCamera`. **Реалізовано (спайк) 2026-07-04:** компасний перемикач north-up↔course-up (іконка 🧭 справа-центр, ROTATION_VECTOR + кутове згладжування), verified на пристрої. Обмеження: азимут без tilt-remap (найкраще тримати телефон пласко); чиста версія (LocationComponent / fusion GPS-курс+компас) — MVP-1 | `08`, `10` §8 (фаза 5) |
| P18 | **Per-type / загальні по місту тотали (region-manifest)** — on-demand (D24) не дає «скільки всього N типу в місті» / точного знаменника; порахувати в CC-BY-пайплайні (`spike/pipeline/`) підсумковий файл на регіон (скан тайлів Volda/Ørsta → лічильники по `bygningstype` + accessible-тотал), покласти на CDN, застосунок тягне раз. Живить: тап-по-іконці типу в плашці «відкрито X з Y» (D28 / `08` §8) + правильний загальний Coverage-% (замість «від завантаженого поблизу»). Мала data-задача (Python), не UI. **РЕАЛІЗОВАНО 2026-07-06** (пайплайн-лічильники + Android fetch/показ + реальний manifest на R2, device-verified: «X of Y» + city-%). Регіон = **КОМУНИ** 1577 Volda + 1520 Ørsta (не міста) — тому числа великі (22 645 буд., 87% досяжних; сільські uthus/hytter уключено). **⚠️ Нац.масштаб:** ОДИН глобальний manifest зламає метрику (знаменник = уся Норвегія → «X з мільйонів», Coverage ~0%) → потрібні **per-region manifests** (per kommune, у великих містах per bydel), застосунок обирає за локацією (GPS→kommune / tile→kommune); пайплайн уже per-kommune, тож емісія per-kommune-manifest природна. Скоуп «своєї території» (kommune vs bydel vs радіус) — рішення при масштабуванні, дотично P3/P10. Пов'язано з P3 (знаменник %), D24 |
| P19 | **«Свіжість» — розкрите тьмяніє без візитів, прохід освіжає** (відповідь на «що робити, коли проходиш повз уже розкрите», Денис 2026-07-06; ЗАРАЗ — варіант A «нічого», розкрите лишається розкритим). Розкрите, куди давно не заходив, поступово **десатурується** (гуе зберігається — принцип 08 §2; це «серпанок» D28 §3), а прохід повз **освіжає** (знову насичене + легкий glow). Дає позитивне підкріплення за повернення + **причину ходити знову** (двигун утримання в малому місті, P10). ⚠️ lifetime-«розкрито» НЕ падає — без штрафу/expiry (D20/D30). **Більша фіча → ПРОТОТИПУВАТИ Й МІРЯТИ** (мотивує чи тисне?), у раунді утримання/гейміфікації, не зараз. Пов'язано з P10, D28 §3, D20/D30 |
| P20 | **Area = tettsted (динамічна поточна) + видимий кордон** (рішення Дениса 2026-07-06, з walk-фідбеку: «0,7% of area» абстрактне без кордону + area завелика = обидві комуни Volda+Ørsta, 19 723 буд.). Coverage-% рахується від **tettsted (міське поселення), у якому ти ЗАРАЗ** (SSB Tettsteder — ліцензія **NLOD**, деталі нижче; НЕ CC-BY), не від комун; **кордон поточного tettsted малюється лінією** на мапі (area стає видимою, не словом; принцип §1 самопояснювальність). При масштабі — кордони **сусідніх tettsteder** теж видно. Пізніше (з системою досягнень) — можливо дрібніше, **grunnkrets**. **Обсяг:** пайплайн (SSB tettsted-межі → per-tettsted лічильники по типах + тег `tettsted_id` на кожну будівлю + boundary-GeoJSON) + застосунок (GPS→tettsted point-in-polygon → per-tettsted %, boundary-шар). **Поза будь-яким tettsted (село / між містами) = «поза містом» без %** (варіант a, Денис 2026-07-06). Розширює P18 (per-region → per-tettsted manifest) + P3 (знаменник = tettsted). **✅ ПОВНІСТЮ В ПРОДАКШНІ 2026-07-11 (пайплайн+Android+R2, end-to-end verified):** data-спайк → джерело **SSB Tettsteder WFS** `kart.ssb.no/api/mapserver/v1/wfs/tettsteder` (`ms:tettsted_2025`, стабільний id `tett_nr`, EPSG:4326, geometry без дірок; Volda 6071 + Ørsta 6081 покриті); **⚠️ ліцензія NLOD, НЕ буквально CC-BY** (відкрита/атрибуція, сумісна; кордони — окремий файл, не в ODbL-тайлах → +© SSB в атрибуцію). Пайплайн: `fetch_tettsteder.py` + `build_tiles.py --tettsteder=` тегує `tettsted_id` (centroid-PIP) + емітить `tettsteder.geojson` (межі + per-tettsted `total`/`accessible`/`byType`; лише data-backed). Android: `TettstedIndex` (CMP-clean PIP+тотали) + `TrackingRepository` (чисельник per-tettsted **із тегу `tettsted_id`** — index-незалежний; `currentTett` GPS→PIP) + `StatusPlate` «% від <tettsted>» / «поза містом» (гейт `tettLocated`) + `MapController` два line-layer (сусіди пунктиром + поточний насичений; feature-state Android нема D7). ⚠️ після регену очистити `filesDir/areas` (старі тайли без тега). **Рішення Дениса 2026-07-11:** кордон — **ЛИШЕ поточний** tettsted (без сусідів); лічильник Revealed — **ЗАВЖДИ глобальний** (per-tettsted лишається тільки %); «поза містом» — нейтральний. **End-to-end верифіковано (Pixel):** повний реген (Matrikkelen+OSM+Elveg+Tettsteder, 416 тайлів, accessible 87.1%) залито на **R2**; очищено кеш → застосунок дотягнув тайли+межі з CDN → плашка «Revealed 222 (**6,2% of Volda**)» (реальний per-tettsted % = 222/3606), теал-кордон Volda видимий, © SSB, без крешу. Деталі → CHANGELOG 2026-07-11. Пов'язано з P18, P3, P10, `08` §1 |
| P21 | **Обʼєм (Площа×Висота) — ціль-так, формула-ні** (з брейнсторму `04-2`, 2026-07-11; opt-in шар D33) — мета «баланс місто↔природа» варта, але **мультиплікативна формула/валюта відхилена** (реінтродукувала б score — S6/D13; degenerate case «місто-only→Обʼєм 0» = докір моно, проти D20). Замість: (а) дві **незалежні шкали** (Coverage + висотні метри/вершини, вже в D13/`03`); (б) **complementary-колекції**, що вимагають обох («Utforsket Volda» = tettsteder + топи навколо); (в) опц. бейдж **«Balansert utforsker»** за баланс, без покарання моно. Сам «Обʼєм» — щонайбільше **декоративний візуал** двох незалежних шкал (без числа/ґейтингу нагород). Post-MVP, opt-in | `04`, D13/D20/D33 |
| P22 | **3D-діорама «мініатюрна Норвегія»** (з `04-2`; opt-in шар D33) — колекція вирощуваних landmark-ів (stavkyrkje/fyr/вершини) як особистий світ; сильний диференційний образ, але real-time 3D = велика нова поверхня для solo-dev + native-OOM ризик MapLibre при динамічних шарах (D32). **Старт: 2D-«samling» з іконок D28** (≈80% ефекту), справжнє 3D — лише після 2D-прототипу + підтвердженого retention. Post-MVP | `04`, D28/D32/D33 |
| P23 | **Типи будівель = «точки дій»** (ідея Дениса 2026-07-11; opt-in шар D33) — тип надає функцію, не лише колір (напр. релігійна будівля → конвертація відкритого в діораму). Гарди: **без хард-гейту прогресу за рідкісним типом** (rural-щільність — анти `09` §11 «порожня мапа»); тактовно/безпечно (підхід з публічного простору D6; повага до діючих церков); мапінг тип→дія осмислений, короткий, опційний. Зчеплено з P22. Post-MVP | `04`, D6/D28/D33 |
| P24 | **Новизна маршруту («нова вулиця сьогодні»)** (доповнення 2026-07-11) — спокійний сигнал, що прямо інструментує центральну метрику успіху **D2** (частка нових сегментів проти повторних; «рухайся інакше знайомим світом»). **Залежить від шару «пройдені вулиці/сегменти» = P6** (поки відкладено) → реалістично fast-follow/post-MVP разом з оцінкою P6. Пасивний відповідник активного авто-маршруту P16; без балів | `04`, D2/P6/P16 |
| P25 | **Історичний шар реальних місць** (з `04-2` §8; post-MVP контент) — відвідав місце → розкриваєш його історичний шар (старі фото/карти, що тут було 100–200 р. тому). Унікально, освітньо, sponsor-friendly. Дані значною мірою відкриті: Riksantikvaren/Kulturminnesøk, Nasjonalbiblioteket (PD-фото/карти), Kartverket historiske kart, lokalhistoriewiki. Радше контент/освіта, ніж механіка; концептуально біля **D27** (POI+нотатки природного шару). Post-MVP | `06`, `04`, D27 |
| P26 | **In-app налаштування / вигляд (Settings-екран)** (ідея Дениса 2026-07-12, з тюнінгу стилю карти) — винести tuning-опції в екран, щоб Денис перемикав сам на пристрої **без ребілду** (пришвидшує власну design-ітерацію фази dogfood: замість «Claude пересобрав+встановив» → «тапнув і порівняв»). Кандидати: **(a) стиль базової карти Kartverket** (`topograatone`/`topo`/`toporaster` + опц. приглушений `topo` через MapLibre `raster-saturation`) — **мале**, лише swap raster-source у `MapController`/style + персист; **(b) кольори типів будівель** — color-picker per тип; ⚠️ живить І мапу (fill-expression `MapController`) І легенду (Compose `BuildingPalette`) + персист + reset-to-default → це **не лише зручність, а accessibility-фіча** (colorblind-користувач обирає власну палітру, WCAG-aligned, `08` §2) — **середнє, окремий інкремент**; **(c) акцент/фон кнопок (FAB)** — мале. Персистенція = DataStore/SharedPreferences; вхід — gear-іконка. Частина лишається dev-tuning, частина може стати справжньою user-персоналізацією. **(a)+(c) можна почати ASAP; (b) — далі.** | `08`, D28, D32 |

> *P9, P11, P12, P14, P15 → перенесені в «Ухвалені» як D26, D27, D28, D29, D30 (2026-06-27). У proposed лишаються **P10** (двигун новизни — прототип+вимір) і **P13** (м'яка гейміфікація) + **P16** (авто-маршрут v2+) + **P17** (bearing-up орієнтація мапи — фаза 5). **Гейміфікація-сесія 2026-07-11 (opt-in шар D33):** +**P21** (Обʼєм-без-формули), **P22** (діорама 2D-first), **P23** (типи-дії), **P24** (новизна маршруту, лінк P6), **P25** (історичний шар). Ідея «3 герої» — відкладено (можливо пізніше), не заведено як P. **2026-07-12:** +**P26** (in-app Settings-екран — стиль карти / кольори типів / акцент).*
