# AGENTS.md — Streif

> Цей файл — **єдине джерело** майстер-контексту проєкту. AI-агенти читають його автоматично щосесії: **Codex** — напряму, **Claude Code** — через `@import` у `CLAUDE.md`.
> Останнє оновлення: 2026-08-07 (повна хронологія — `docs/CHANGELOG.md`)

---

## Як працювати над цим проєктом (інструкції для AI-агента)

- **Мова спілкування й документів — українська.** Норвезькі власні назви (Kartverket, bokmål, nynorsk, varsom, fjell, bydeler) і технічні терміни (PostGIS, MapLibre, offline-first, GDPR) лишаються як є.
- **Процес:** AI-агент пропонує структуру/чернетку → користувач (Денис) коригує. Не вивалювати готове без обговорення спірних рішень.
- **Не змінювати наявні файли чи код без явного запиту.** Не додавати того, про що не просили.
- **Питання з варіантами — кнопками** (перевірено 2026-07-28: інтерактивні опитування в Claude Code працюють; попереднє правило «кнопок тут немає, питай текстом» було хибним і знято). Формат, який Денис просив: **по одному питанню за раз**, варіанти відповіді кнопками, у кожному варіанті — наслідок, і явна позначка, який варіант рекомендує агент. Розлогий розбір — текстом ДО кнопок, самі кнопки — короткі.
- Для технічних рішень — звірятися з актуальною документацією (Kartverket, MapLibre, Android, Laravel), не покладатися лише на пам'ять.
- **Після кожної великої зміни — незалежне cross-model review (codex), потім пропозиція власнику на основі його висновків** (**D40**, правило Дениса від 2026-07-31). Велика зміна = нова механіка · зміна моделі даних · зміна того, що йде в мережу · новий шар. ⚠️ Знахідки codex **верифікувати по коду, не приймати на віру** — у D23.1 із 18 підтвердилось 16, дві спростовано. Найцінніше він дає **до** розробки, на дизайні й правилах, не лише на дифах. Операційні деталі виклику (модель, рівень міркування) → **D40**.

---

## Про проєкт

**Streif** — Android-застосунок, що гейміфікує щоденне дослідження місцевості в Норвегії. Користувач «розкриває» власну мапу: розкриті об'єкти (будинки, стежки, вершини) засвічуються кольором на повністю видимій карті, побудованій на відкритих державних картографічних даних (Kartverket).

- **Робоча назва:** Streif (фінальний бренд — TBD; домен `streif.no` у переговорах; фолбеки: Streifa, Omvei).
- **Платформа:** Android-first (Kotlin + Jetpack Compose). iOS відкладено.
- **Позиціонування:** «Walkable для Норвегії, але глибший» — та сама механіка розкривання, але на якісніших держданих і одразу місто + природа.
- **Власник:** Денис Семенюк — solo-розробник (ENK), Ørsta/Volda, Sunnmøre.

---

## Поточний статус

> ⚠️ **Цей блок — стислий дайджест.** Повна, актуальна хронологія «що+коли+чому» — у **`CHANGELOG.md`** (єдине джерело живого статусу); операційний стан+команди → `spike/android-render/SPIKE-STATUS.md`. Тут не дублюємо подієвий журнал, щоб не розходився.

- **Фаза:** виконання — **MVP-0 зібрано; Compose-UI (D32) злито в `main`; P18 (city-тотали) реалізовано; P20 (Coverage=tettsted) — ✅ повністю в продакшні (пайплайн+Android+R2, end-to-end verified); Nature-v1 (D34) POI-«collect» points-only — ЗБУДОВАНО (інкр. 1-4 + фото), рендер+картка device-verified, walk-зарахування ✅ польово підтверджено (2026-07-13)**. **Польовий walk-тест Compose-UI ✅ ПРОЙДЕНО** (2026-07-20: 2 сесії, ~3 км, 134 розкриття за вечір; 383 розкриття всього). Синк із Drive і реліз-пакет A — ✅ зроблено (див. нижче).
- **Зроблено (стисло; деталі → `CHANGELOG.md`):** Spike-1 рендер (D21/D22) · Spike-2 трекінг-ядро Stage A/B/C — **battery Stage C ПРОЙДЕНО** (польовий тест 2026-07-05, ~6-8%/год екран-off) · on-demand дані (D24) + edge-матчинг (D25/D25.1) · MVP-0: Room (D11), eligibility (D6), onboarding, локалізація (D18), компас (P17) · **CC-BY продакшн-дані живі на Cloudflare R2** (OSM+Matrikkelen, `CdnGeoJsonAreaSource`/`USE_CDN`, D31) + Elveg-eligibility (D31) · Compose+M3 UI (D32) · польовий тест 2 телефони (recall 100% / precision 87%, P2) · region-manifest «X з Y» + city-Coverage-% (P18) · **Coverage-% per tettsted + видимий кордон (P20; SSB Tettsteder NLOD, пайплайн+Android+R2, end-to-end verified на Pixel: «6,2% of Volda» з CDN)** · **Nature-v1 (D34): POI-«collect» points-only — пайплайн `fetch_poi`/`build_poi`→`poi.geojson` (+R2) · рендер SymbolLayer+CircleLayer (Walksy-стиль) · proximity-зарахування R=35м + Room `poi_visits`/MIGRATION_1_2 · калм-кор картка POI + іконка режиму місто/природа · фото на картці (Wikidata P18→Wikimedia Commons; 4/11 міських POI; `--city-only` тест-allowlist)** · **P26: Settings-екран + перемикач стилю карти (a) ЗБУДОВАНО** (gear-FAB → 4 стилі Kartverket, swap на льоту + персист; device-verified).
- **Незалежне review (D23):** ✅ виконано 2026-07-01 — «готово до MVP-0 з умовами» (деталі → `CHANGELOG.md` 2026-07-01 + `DECISIONS.md` **D23.1**; brief-архів → `docs/REVIEW-D23-BRIEF.md`).
- **Nature-v1 наповнення + режим (D35, 2026-07-18):** POI-набір **11 → 71** на R2 (26 вершин · 15 краєвидів · **15 badeplass** із Naturbase NLOD · 7 gapahuk · 6 статуй · 2 хижки; церкви прибрано — правило **«POI ≠ будинок»**, вони лишаються фіолетовими будинками) · safety-allowlist природних POI (≤60 м від стежки · sac ≤ T2 · ele ≤ 1000) · **авто-режим місто↔природа** з гістерезисом (буфер ±100 м + 3 фікси) · режим дає **safety-контекст + акцент даних** · **маркер «я» = стрілка + коло точності в метрах**. ⛔ **Nasjonal Turbase (DNT API) мертвий** — не розглядати. Збірка+41 тест ✅; **польова прогулянка ✅ відбулась 2026-07-20** (~3 км, 134 розкриття).
- **P30 екран статистики (2026-07-19):** **✅ ЗБУДОВАНО** — bottom-sheet «Progress» (патерн P26): **Coverage + Variety** по географії × типу, зріз **tettsted ↔ комуна** (поза містом — комунний), чесні `accessible`-знаменники (D6), без score (D13). Побічно з'явилась **нова географічна одиниця — комуна (P31)**: тег `kommune` у тайлах + `byKommune` у manifest (Volda 10 812/9 180 · Ørsta 11 833/10 543), залито на R2. Під капотом: `RevealCounters` (чисельники «одиниця × тип» із Room), **Room-міграція 2→3**, гео-контекст без старту прогулянки. Device-verified: Volda «190 з 3606 · 5,3%» / комуна «190 з 9180 · 2,1%», 249 розкриттів пережили міграцію; 121 тест ✅.
- **P30 тюнінг за польовим користуванням (2026-07-27):** **✅ ЗБУДОВАНО й device-verified** — (1) вкладки Tettsted/Kommune більше не міняються місцями (порядок фіксований; `ordinal` у `StatsScope` став load-bearing у двох місцях); (2) «Samlinger» показує колекції лише обраної категорії (`CollectionsSectionModel.scoped`, презентаційний фільтр — зарахування вкладок не бачить, D30; заразом стеля `MAX_FEATURED` стала поключовою за типом одиниці, інакше комунні витісняли б усі поселенські); (3) окремі chrome-гліфи `ic_scope_tettsted`/`ic_scope_kommune` поза `IconSet`. Правка «набори іконок для будівель» **знята**: іконки будівель уже під перемикачем наборів (не хардкод), на мапі ж гліфів будівель нема взагалі — там `FillLayer`. 306 тестів ✅.
- **Синк із Drive — ✅ ЖИВИЙ і device-verified (2026-07-21):** OAuth-клієнти створено, `default_web_client_id` вписано, вхід пройшов, `backup_last_ts` записано (копія реально лягла в Drive). Пакет A під закритий тест зібрано (`670cc93`); **локальну кнопку «видалити все» прибрано свідомо** — рішення власника (D38): у застосунку лишається видалення копії з Drive, локальне стирається **видаленням застосунку** (надійно завдяки `allowBackup="false"`).
- **Цілісність копії в Drive — ✅ полагоджено (2026-07-27, `4b162f9`):** §B.6 **#13** (`pull()` знищував дублікат файла копії не прочитавши → при двох телефонах прогріс зникав назавжди в сценарії переїзду; тепер `pullAll()` читає всі, `BackupMerge` зливає, а зайві прибирає **лише** успішний запис об'єднаного), **#12** (вимкнення бекапу тепер питає, що робити з копією — рішення Дениса, варіант «а»), **#14** (рядок «Твої дані» більше не каже «only on this phone» тому, у кого бекап увімкнено). 310 тестів ✅.
- **§B.6 закрито ПОВНІСТЮ (2026-07-27, `941b0aa`):** #15 (абсолют у disclosure), #16 (мертві DAO `clear()`), #17 (§8→§7, звірено за самою політикою — і побічно виявлено, що в ній **нема розділу 6**), #11 (`<device-transfer>` — перевірено за офіційною документацією: `allowBackup="false"` D2D **не** вимикає). Заразом переписано **`play-fgs-declaration.md` §2.1**: там було не одне, а **три** неправдиві твердження, і два з них — ті самі, що рецензент уже ловив як BLOCKER у політиці, просто в цьому файлі їх тоді не поправили. Дефектів review відкритих **не лишилось**.
- **Телеметрія тесту (D39) — підхід 1 ЗБУДОВАНО (2026-07-27, `4387372`):** тека `telemetry/`, три гейти (`BuildConfig.TELEMETRY` · перемикач · непорожній `TELEMETRY_URL`), екран згоди при першому запуску, режим міток відкрито тестерам. Ключове: **майже нічого не інструментовано** — підсумки читаються з `sessions`/`visits` раз на добу, живі лічильники лише для мережі й синку. Гард D14 закрито тестом (жодне поле моделі й жоден ключ JSON не може називатись як координата). 329 тестів ✅. **Device-verified 2026-07-29** разом із T7 (секція «Closed test», перемикач, номер встановлення); `TELEMETRY_URL` порожній → у мережу не ходить.
- **Реліз-обв'язка (2026-07-29):** **✅ три кроки зроблено** — (1) **§B.6.5 T7**: номер встановлення видно в Settings (`7262006`), лише при ввімкненому зборі, з виділенням довгим тапом; без нього обіцянка політики п. 10a / data-safety §2.5 «назвіть номер — і надіслане видалять» була невиконанною. Device-verified, заразом закрито «ще не бачене на пристрої» з T1. (2) **§B.0**: `applicationId`+`namespace` = **`app.streif`** (`3495194`), 121 файл через `git mv`, маніфест правок не потребував (`.MainActivity` + `${applicationId}.fileprovider`). **Дані з телефона перенесено й перевірено** — копію знято ПЕРЕД правкою (з `-wal`, `integrity ok`), розпаковано в новий пакет через `run-as`, новий застосунок показує ті самі **494 розкриття**; старий лишився на телефоні під ярликом **«Streif old»**. (3) **§B.5**: `bundleRelease` дає підписаний `.aab` (36,2 МБ, RSA-4096, ключ дійсний до 2053), `lintVitalRelease` зелений. 329 тестів ✅.
- **OAuth під новим id — ✅ закрито 2026-07-29 (device-verified):** Денис створив Android-клієнти на `app.streif` (debug + upload SHA-1), і після **виходу й повторного входу** синк ожив — `backup_last_ts` рухається, `syncOk: 5 · syncFail: 0 · syncDup: 0`, розкриття недоторкані. ⚠️ Урок у §B.6.3 #6: клієнтів **не досить** — згода на `drive.appdata` прив'язана до ідентифікатора застосунку, тож перелогін обов'язковий; а збій при цьому **беззвучний** (мовчазний токен → `NotSignedIn` → toast не показується), і виглядає як «кнопка нічого не робить».
- **Подано в Play + телеметрія жива — ✅ 2026-07-30 (деталі → `CHANGELOG` 2026-07-29 (2)):** `.aab` (`versionCode 3`) на треках, **15 змін надіслано, стан «Перевіряється»**; усі декларації «Контент додатків» заповнені (FGS звірено окремо — його **немає** в списку надісланих змін, тобто той список не є переліком декларацій). Застосунок **встановлено з Play** і **494 розкриття відновлено з Drive** — перша реальна перевірка OAuth-клієнта на ключ Play і шляху відновлення в не-debuggable збірці; злиття, не заміна (`syncRestored: 1`, стало **545**). Опублікована політика приватності звірена з репозиторієм **байт-у-байт**. Воркер телеметрії задеплоєно, **16 пакетів прочитано**; читання тепер через сам воркер (`/list`+`/get` під окремим адмін-секретом, fail closed), boto3 й S3-ключі R2 більше не потрібні.
- **Черга §10a: A і B зроблено 2026-07-31 (`10-roadmap.md` §10a).** **A** — пробіл §B.6.3 #18 полагоджено в конкатенації `StatusPlate` (не в ресурсі), перевірено `aapt2 dump strings` на скомпільованому APK; кадри для магазину чекають оновлення телефона з Play (зібрано **`versionCode 4`** на внутрішній трек — локальна збірка поверх Play-версії не стає, різні ключі підпису, а `run-as` до неї не пускає). **B** — P32 варіантом «а». Далі — **D** стежки (лінійна механіка D34). У черзі також **C** розбір 4 хибних розкриттів із телеметрії й **E** прибирання робочих тек (інвентар готовий, ⛔ **без рішення Дениса нічого не видаляти**). Пасивно чекаємо: вердикт Play · відповідь тестерів про невідомий OnePlus 8 Pro (джерело 12 порожніх пакетів не встановлене — гіпотезу про pre-launch report перевірено й відкинуто) · перший справжній пакет телеметрії.
- **Окремо відкрите (не в §10a):** **третій OAuth-клієнт** — SHA-1 із Play App Signing; без нього вхід беззвучно впаде на Android 7-8, які перевіряються **попереднім** ключем підпису (§B.6.3 #6). Post-MVP — більше польових маршрутів (генералізація тюнінгу поза Volda) · custom-domain R2 · per-region manifests (нац.масштаб).
- **Gate перед розробкою (Фаза 0):** ✅ пройдено.
- **MVP-напрям:** місто-first, device-local v1 (зафіксовано).
- **Планування (2026-06-26/27):** конкурентний аналіз 20 застосунків-аналогів (глоб.+норвезькі) → зафіксовано **D26–D30** (deferred auth, POI у природному шарі, мінімалістична візуальна мова, UX точності GPS, захист від ретроактиву) + дизайн-референси (`08` §14) + анти-патерни жанру (`09` §11). Нові відкриті: **P10** (двигун новизни/утримання малих міст), **P13** (м'яка гейміфікація), **P16** (авто-маршрут v2+).
- **Версійний контроль:** проєкт під **git** від 2026-06-27 (гілка `main`); `CLAUDE.md` = імпорт `@AGENTS.md` (єдине джерело).

---

## Ключові рішення (зафіксовані)

**Механіка.** Зафарбовування розкритих об'єктів на **повністю видимій** карті. НЕ туман (fog-of-war) — туман приховував би рельєф/небезпеки і суперечив би безпеці. Об'єкти сірі за замовчуванням → набувають кольору за типом при розкритті (UI-мова «розкрито/побачено», D6). Ціль (post-MVP): опційний «серпанок» — легка десатурація недослідженого (+ «свіжість» розкритого, P19); без приховування рельєфу.

**Скоуп.** Гібрид місто + природа — це **візія**. Виконання **поетапне**: MVP запускає один шар end-to-end, решта каркасом. Три шари: (1) Місто — будинки/bydeler; (2) Стежки — turstier, км/висота; (3) Вершини — топи/DNT-хижки. Архітектура (багатошарова модель даних) підтримує всі три з дня 1; MVP — device-local через CLI-пайплайн tiles без backend/БД (PostGIS — лише v2, `DECISIONS.md` D12).

**MVP-виконання (зафіксовано 2026-06-20, деталі в `05`).** Перший реліз — **місто-first** (провізорно; формальний лок у `09`) і **device-local** (без логіну/живого backend — швидше тестувати). Рендер зафарбовування — **двошаровий** (сіра база + visited GeoJSON-overlay; Android-`feature-state` недоступний → двошаровий єдиний, D7/D22). Дані будинків — відкриті **INSPIRE Core2d (геометрія) + Matrikkelen (тип), CC-BY 4.0**, напряму з Geonorge (`building2osm` WFS зламаний — D8; spike тимчасово на OSM).

**Після Фази-0 (review + тех-стек-пас, 2026-06-21).** Backend **прибрано з MVP** (CLI-пайплайн tiles, без БД); матчинг уточнено (буфер сегмента, accuracy, eligibility «доступний з пішої мережі»); метрики — три виміри **Coverage/Variety/Discovery** (без score); spike **розділено** (симульований GPS → реальний FGS); UI-мова «**розкрито**»; реframe — «**мапа новизни**», метрика успіху = «**новий маршрут**». **Канонічний журнал рішень — `docs/DECISIONS.md`.**

**Spike-2 (2026-06-22…26, деталі в `CHANGELOG.md`).** Дві зміни архітектури за польовими даними:
- **D24 — on-demand area download** (замінює фіксований бандл): `BuildingStore` інкрементальний, `AreaLoader` тягне будинки в bbox навколо GPS на льоту + локальний кеш. **Тест-джерело = Runtime Overpass (OSM/ODbL)**; ⚠️ **перед публічним релізом ОБОВ'ЯЗКОВО мігрувати на pre-hosted CC-BY CDN** (надійність + масштаб + ліцензія). NB: це **новий вихідний мережевий запит** (bbox-координати до Overpass) — сирий GPS-трек усе одно не залишає пристрій (D14), але потік зон задокументовано в `07`.
- **D25 — матчинг до КОНТУРУ будинку (edge-distance) + closest-approach gate** (замінює **матчинг-частину** D5 — центроїд-радіус; ActivityGate-частина D5 чинна): центроїд спричиняв і «зарано», і пропуск через дорогу/видовжених. `R_FAR=18`/`MIN_EDGE=8`/`SLACK=1.5`, bbox-grid, локальна проєкція. Польово-верифіковано.

**Планування за конкурентним аналізом (2026-06-27, деталі — `08`/`09`/`DECISIONS.md`).** З огляду 20 аналогів: ніша «розкривання повністю видимої мапи за типом об'єкта» у Норвегії **вільна**; найближчий конкурент за гейміфікацією — DNT/UT (SjekkUT); фандинг-прецедент — Sparebanken Møre→StikkUT (2 млн kr). Нові ухвалені: **D26** deferred auth (логін після цінності), **D27** POI «цікаві місця» (читати з відкритих даних → особисті локальні нотатки → публічні відгуки v2) — у природному шарі, **D28** мінімалістична візуальна мова (контурні іконки колір-за-типом, без перевантаження, collect/unlock **без абстрактного score**), **D29** UX точності GPS (не розкривати тихо), **D30** захист від ретроактиву. Топ-болі жанру (наш «не наступити») → `09` §11.

**Нагорода = ЗАКРИТТЯ НАБОРУ (D41, 2026-07-31).** Єдине правило, з якого виводяться всі колекції: не за дію, не за кількість; складність виражає структура набору (D13). ⚠️ **Механізм уже збудований і живе в тестовій збірці** (`ui/Collections.kt`, секція «Samlinger»): набори генеруються **правилом** (`CollectionRules`, масштаб 5..1000), назви `bt`-кодів — зі згенерованого `bygningstype.json`, **ручної курації не потрібно**; знімок знаменника, «бонус після закриття», гард видимості й чесність про покриття кодами — теж уже в коді. Сім ухвал однією сесією: **`capped ×2` знято** (стеля-множник суперечила «rarity ніколи не множник»; 112 `bt`-кодів виражають рідкість структурою) · **бейдж «Balansert utforsker» викинуто** (баланс — співвідношення, не закриття) · заморозка складу — **лише при закритті** · **«Перші кроки» лишаються винятком, названим і обмеженим** · **штамп = позначка, нагорода = закрита сторінка** · додаємо **`lastSeenTs`** (без нього мертві сезонні листівки й P19) · наступне — **альбом P22**, не нові набори. Відхилено: «зібрано N нагород», відсоткові пороги, квартал/вулиця (у тайлі немає вулиці). → `DECISIONS.md` **D41**, `04` §5/§11.

**ПОЛЬОВА ПРОГУЛЯНКА СТЕЖКАМИ ВІДБУЛАСЬ (2026-08-06 вечір, на `versionCode 22`) — 9 зауважень Дениса; ✅ ЗАУВАГИ 1-4 ЗАКРИТО того ж дня** (`CHANGELOG` 2026-08-06 (після прогулянки)). Механіка працює (`Prestholmen · gått`), але мовчала — це й полагоджено: частковий прохід тепер видно і в блоці, і на картці, і **на мапі буквально** (пройдені десятини малюються суцільним слідом — рішення Дениса; заразом це закриває «ефект зафарбовування не читається») · базовий колір стежки `#B0ABA4` → `#63605C` (контраст до підложки 1,24 → 3,41) і `applyAccent` нарешті чіпає стежки в режимі «природа» · сума типів більше не сперечається з «Avdekket her». ⚠️ **У полі ще не перевірено** — поїде наступним бандлом. **Лишились зауваги 5-9**: маршрут Volda під вкладкою Ørsta (лікувати ПОДАЧЕЮ, не знаменником) · «3 m · 1,2 m/s» із плашки · перебудова верхньої панелі (профіль замість шестерні) · відвіданий POI не читається як відвіданий (з **P35**) · стежок замало, фільтрувати нові за характеристиками з даних. Повний перелік → `CHANGELOG` 2026-08-06 (вечір).

**✅ ВЕРХНЯ ПАНЕЛЬ ПЕРЕБУДОВАНА (заувага 7, 2026-08-07):** профіль-аватар справа вгорі → меню (акаунт · давність копії · увійти/синк/вийти · «Налаштування»); зі стека FAB пішли статистика й шестерня — межа тепер **по змісту**: стек про МАПУ, аватар про застосунок; плашка дістала рамку й гліф статистики (вона відкривала шторку ще з P31, але виглядала як напис — прибрати FAB, не показавши цього, означало б сховати вхід). ⚠️ **Аватар = справжнє фото акаунта** (рішення Дениса) → **четвертий вихідний адресат `googleusercontent.com`**, декларації оновлені (`play-data-safety` 3→4 + §2.4 + §3; політика п.4 і п.10). ⛔ **За Денисом:** оновити **опубліковану** політику (розійшлась із репо) і додати **Photos → Profile photo** у Play Console. Review (D40) дало 5 знахідок, усі закрито; найтонша — `MAX_CHIPS` лишався відкаліброваним під стару ширину плашки (6 → 4). **Жива 24 додала ще три правки (`versionCode 25`):** зауваги **5/6/8** (масштаб блока стежок — у заголовок; «3 m · 1,2 m/s» прибрано з плашки, але СТАН лишився — це D29; обводка зібраного POI стала чорнильною, тобто **та сама мова, що в стежок**) · **вихід названо «Logg ut»**, а не «вимкнути копію» — питання про копію лишилось усередині, бо після виходу токена немає й прибрати її застосунок уже не зможе (§B.6 #12) · **тиха дозаписка фото** для тих, хто ввійшов ДО 24 (інакше літера назавжди в кожного тестера). → `CHANGELOG` 2026-08-07 (2).

**✅ П'ЯТЬ ЗНАХІДОК ПРО ВТРАТУ ПРОГРЕСУ ЗАКРИТО (2026-08-07): #11 · #3 · #2 · #4 · #6** (`CHANGELOG` 2026-08-07). Правило «накладати знімок, а не присвоювати» винесене в чисту `Trails.mergeProgress` — бо два виклики мусять підкорятися одному правилу, а доти кожен робив його сам і другий забув · відновлення в Room стало **монотонним** (`merge`, не `@Upsert`: маска — стан, що росте, тож заміна цілим рядком її вкорочує) · `SCHEMA_VERSION` 1→2 **+ гард «новішу копію не перезаписуємо»** (не лікує вже випущені клієнти — рятує наступне поле) · знімок «маршрут закрито» живе в **наявній** таблиці `collection_closures` під ключем `TRAIL|<trailId>`, який `parse` відкидає (рішення Дениса; окрема таблиця = нове поле в копії = повторення #4). ⚠️ **#2 лишився без автотесту** — `BackupRepository` ходить у Room, а JVM-тести без Robolectric. **Незалежне review цих правок (D40) дало 5 знахідок, 3 полагоджено того ж дня**, і головна з них — **«#6 закрито НЕ до кінця»**: знімок жив у ядрі, а `TrailRow`/`TrailCardInfo` рахували `walked >= segments` самі, тобто шторка й картка показували закритий маршрут незавершеним (той самий клас, що «лексика подорожує рядками» в G1 — рішення, яке можна перерахувати на межі, колись розійдеться). Друга — **заблокований гардом запис рапортував УСПІХ** («Прогрес збережено» при порожньому записі в Drive). Третя — **неповно розібраний набір дарував незароблене закриття** (тепер `TrailStore.skipped`). Дві лишились свідомо: гард не змінює поведінки вже випущених клієнтів, а неатомарність маски й closure самолікується при рестарті (`dbIo` — single-thread FIFO). **457 тестів ✅ · `versionCode 24`.** Лишилось із 13: **чотири** — #8 і #10 (ПОЛЬОВІ, разом із порогами) · #5 (не лікується) + #1 → P41.

⛔ **Історія вибору (Денис, 2026-08-06): починали з п'яти знахідок про ВТРАТУ ПРОГРЕСУ — #6 · #3 · #4 · #2 · #11.** Підстава вибору, а не сам вибір, тут важлива: решта відкритого ділиться на **польове** й **не польове**. Пороги (`CORRIDOR_M`, 8/10), **#8** (один фікс зараховується всім кандидатам у коридорі — при максимальній точності це радіус 55 м) і **#10** (джитер на місці) — питання, на які відповідає **прогулянка**, і робити їх наосліп означало б повторити ту саму помилку, через яку пороги й «взяті з голови». ⚠️ Новий рендер зробив їх **видимими**: зафарбовані шматки там, де людина не йшла, тепер помітні на мапі, тож прогулянка перевіряє не одне, а три. А п'ятірка вище доводиться **читанням коду**, не залежить від прогулянки й порушує тверде правило **D30**; лікується знімком знаменника — прецедент є (**P28**). ⚠️ Із них **#4** єдина, що губить прогрес **назавжди**.

⛔ **БОРГ: 4 знахідки codex-review стежок із 13, і ТРИ з них польові.** Полагоджено **#12** (польова заувага 1) · **#9** (перебудова 498 фіч на кожен кошик) · **#11 · #3 · #2 · #4 · #6** (п'ятірка про втрату прогресу) · **#7 · #13** — усе 2026-08-07. **#7** був вартий уваги окремо: фікси до приходу `trails.geojson` губились назавжди (для зон будівель replay є з C2/D23 #5, для стежок його забули), і б'є це саме там, де людина відкриває застосунок і одразу йде — тобто на початку кожної прогулянки. Це окремий кандидат-пояснення, чому на `Indre-Hovden` зарахувалось так мало. ⚠️ **Три з чотирьох, що лишились, — ПОЛЬОВІ**, і саме тому не зроблені: **#8** (один фікс зараховується всім кандидатам у коридорі — при максимальній точності радіус 55 м) · **#10** (джитер на місці) · разом із порогами `CORRIDOR_M`/8-10 їх має розсудити прогулянка, а новий рендер робить їх **видимими**. **#5** (стабільність `segment_id` при перезніманні Kartverket) — записана реальність, не лікується. ⚠️ Знахідку **#1** («D14 порушено») я **перевірив і поправив**: це властивість моделі з першого дня, не дефект стежок → **P41**.

**Друге review стежок (D40, `sol`/xhigh, 2026-08-06 після правок видимості): 5 знахідок, усі закрито того ж дня** — гарячий шлях (див. вище) · блок не оновлювався на ходу · картка була знімком на момент тапу · «0,0 km gått» поруч із видимим слідом · 5%-поріг примітки про безтипні розкриття (системна помилка: таких будівель стабільно ~4,1%, тобто примітка мовчала б рівно тоді, коли історія росте). Деталі → `CHANGELOG` 2026-08-06 (після прогулянки).

**Стежки ЗБУДОВАНО (D34, 2026-08-06, `versionCode 22`).** Усі п'ять інкрементів `TRAILS-BRIEF`: дані з Turrutebasen (**34 маршрути · 498 ділянок · 85,5 км**, залито на R2 й перевірено через CDN) · зарахування «пройшов уздовж» **10 кошиками вздовж ділянки** (крок 10% = кошик; out-and-back не рахується двічі за побудовою) · Room **4→5** + маска через побітове АБО в SQL · у копії Drive маски **об'єднуються** (не «виграє новіша» — прогрес це накопичення, а не подія) · двошаровий `LineLayer` (⚠️ **перероблено 2026-08-06 після прогулянки**: шари розділені фільтром `done`, і «пройдене» — це самі пройдені десятини окремими фічами, а не вся ділянка з прозорістю за відсотком) · картка маршруту на тап · блок у шторці. ⚠️ **Пороги** (`CORRIDOR_M=25`, «пройдено» 8/10) взяті з голови й **польова прогулянка їх не спростувала і не підтвердила** — вона впала на тому, що прогресу не було ВИДНО; справжня перевірка порогів попереду. ⚠️ D34 «стежки лише поза містом» **не реалізовано** (немає полігонів поселень — та сама причина, що P38). ⚠️ Блок у шторці **не прив'язаний до одиниці**: у даних стежок немає тегів tettsted/kommune. → `CHANGELOG` 2026-08-06.

**Альбом — одне місце за раз (2026-08-05, запит Дениса).** Заголовок-перемикач + перелік одиниць, **ті самі контроли**, що у шторці статистики (`UnitHeader`/`UnitRow` стали `internal`, прецедент `ScopeChip`). Доти всі одиниці зрізу стояли стосом і та сама нагорода повторювалась по разу на місце. `AlbumPage` дістав `unitId`, бо фільтрувати за назвою не можна — поселення й комуна майже завжди тезки. → `CHANGELOG` 2026-08-05.

**Нагороди збудовані (2026-08-01, `versionCode 9`).** Альбом P22 (сторінки згорнуті, секції за одиницею, чип-фільтр) · **8 слот-наборів** «знайди по одному з кожної ролі» (§13; каталог відібрано ДАНИМИ — 6 із 8 існують у всіх 27 комунах) · **святкування закриття** (§12.4), яке за всю історію застосунку не виконувалось жодного разу. ⚠️ Спрацьовує лише на **живому** розкритті: `noteClosures()` кличеться з п'яти місць, і в чотирьох набір закривається бо **приїхали дані**. Межа **D20-A** (`04` §13.7) — коли не почате показувати можна; наш перший аргумент був раціоналізацією. Лишились: **іконки нагород** (чекають P33) і **повні сети** («усі церкви» — кандидати в §13.4).

**Категорії будівель (G1) — СХЕМА УХВАЛЕНА 2026-08-02, ПАЛІТРА УХВАЛЕНА 2026-08-05 (варіант A).**
Палітра — **12 кольорів + обов'язкове обведення** (D43): normal 17,83 · deutan 10,58 · protan 10,49
· tritan 8,39. Єдина правка до виміряного A — трав'яний `Bolig` `#3B6A07` замість оливкового:
перебір коридору спростував записану претензію «інакше не можна», і проти POI він кращий у кожному
режимі. Претензія «`Sakral` став синім» перевірена й **не лікується** — лишається свідомо.
⚠️ Перед фіксацією закрито три виміри, яких §15.11.7 вимагало: контраст до **найтемнішої** підложки
(недоборів 7 із 12, але для A це гілка «АБО обведення») · режим «природа» 0,55 (deutan падає до
5,80 — там колір типу не несе) · **об'єднаний набір із POI**, де сьогодні ТРИ пари збігаються
байт-у-байт і A це прибирає (0,00 → 9,03). Перемикач порівняння знято; колір живе на самій
`BuildingCategory`. Інструмент — `spike/palette/palette_check.py` + `palette_curate.py`.
⚠️ **`run-as` на телефоні більше не працює** — там збірка з Play, підписана ключем Play App Signing;
локальний APK поверх неї не стає. Шлях на пристрій — публікація на внутрішній трек.
Нижче — історія того, як до цього дійшли.

**Категорії будівель (G1) — як ухвалювалось.** Обґрунтування нинішніх 6 **не існувало ніде**; тепер є схема відбору (`08` §15). Ухвалено варіант Дениса «Semden 12» + дві заміни (прибрано окремий `Rekkehus`, відділено `Industri/energi`) = **11 змістових + `Andre`** (§15.11, перевірено двічі: 129 кодів рівно, перетинів нуль, суми сходяться з маніфестом). ⚠️ **Спайк візерунків — частковий провал** (§15.8): розріджені мотиви зникають саме на `enebolig`/`garasje` (127 тис. будівель зі 194), причина структурна. Тому **колір лишається єдиним носієм типу** — і саме тут провал: **палітра §15.10 непридатна**, під deuteranopia/protanopia мінімальна ΔE падає з 17,13 до **1,86** (магазин і церква стають одним синім), плюс 5 із 12 кольорів не тримають WCAG 3:1 на підложці. Обмежує **не число 12, а комбінація «12 кольорів + контраст до світлої підложки»**. Три виміряні шляхи (12+обведення · 10 кольорів · прийняти) → §15.11.2; **обрано перший** (D43, вище).
**Збудовано (`versionCode` 13):** `BuildingCategory.kt` = єдине джерело **схеми й палітри** (колір лежить на самій категорії) · перемикач наборів **знято** разом із персистом і рядками ×3 мови · мапа фарбується з сирого `bt` · обведення **безумовне** · візерунок лишається **опцією під дальтонізм**. Канон рішень — **D42** (схема) і **D43** (палітра). **Рівень «б» ЗАКРИТО (2026-08-05, `versionCode 15`):** рахунок, підписи ×3 мови, гліфи в дію, обчислювані хром-тони, стеля чипів плашки, міграція ключів закриття. Гліфи на всі 12 знайшлись у **всіх трьох** бібліотеках, тож перемикач наборів лишається повним. Наступне — **стежки** (D34, нічим не заблоковані з 2026-07-28). ⚠️ Рівень «б» **не безкоштовний**: на пристрої перевірено, що `collection_closures` **непорожня** (5 наборів `fullført`), а ключ закриття містить назву категорії — тобто без міграції це відібрало б зароблене (D30). Знімки стану «до» — `OneDrive\Streif-device-backuplbum-before-2026-08-05\`.
⚠️ **Виправлення канону:** попереднє «зміна категорій = перезбірка тайлів, заблоковано заморозкою» — **неправда**. Тайл уже несе сирий `bt`, заморожені агрегати несуть знаменники по кодах на всіх трьох рівнях → категорія рахується на клієнті, R2 не чіпається, §B.7 не порушується. Робота розпадається на **три рівні** (§15.11.4): (а) палітра+мапа — нульовий вплив на колекції · (б) лексика колекцій на `bt` · (в) перезбірка тайлів — ⛔ лише вона порушує §B.7. `DataVersion` не чіпати (підйом спорожнив би кеш, на якому тримається P32).

**Гейміфікація — кор + опційний шар (D33, 2026-07-11).** Замість «спокій vs гейміфікація» — **спокійний кор** (дефолт, MVP; D13/D20) **+ опційний шар, що вмикається** (post-MVP): діорама (2D-first), типи-як-точки-дій, баланс місто↔природа **без формули/score**, новизна маршруту, історичний шар, **свіжість** (розкрите тьмяніє/освіжається при revisit). Гарди: безпека не opt-in, без втрати прогресу (D30), без streak/FOMO. Механіки → `04` §11 / `DECISIONS.md` **D33, P19 + P21–P25**; Coverage-% = **per tettsted (P20)**. Брейнсторм `04-gamification2.md` влито в `04`.

**Розкривання об'єктів — 2 рівноправні стовпи Місто/Природа, механіка за геометрією (D34, 2026-07-12; дизайн-сесія → codex-звуження → Nature-v1 points-only ЗБУДОВАНО, інкр. 1-4 + фото).** Кор ambient-reveal + «збирай»-механіка (turmål/POI/вершини/хижки з відкритих даних; стежки=Turrutebasen) — розширення ідеї «наповнення прогулянок цікавими місцями» (Stikk UT-стиль). Геометрія→механіка: площа=edge(D25) · лінія=стежки «пройшов уздовж» (10%+«пройдено»@75%, **ще не збудовано**) · точка=proximity(+фото/нотатка). **Іконка режиму** (тап=ручне перемикання). **Збудовано v1:** POI-точки (рендер+картка+фото device-verified; **proximity-зарахування польово підтверджено** — 3 зарахування 2026-07-13 за 4 хв на ходу). Обидва режими на **тест ASAP** (safety=release-gate, не test-gate). Дані verified (Turrutebasen+OSM), НЕ скрейпити бази Stikk UT/MoroTur. 8 ухвал + черга + курація-адмінка(v2) → `DECISIONS.md` **D34** / `03` §C-D / `CHANGELOG` 2026-07-12.

**Аудиторія.** Первинна (dogfood) — сам Денис (місто + біг + гори в одній людині). Вторинні — бігун, новоприбулий/іммігрант, сім'я з дітьми. Зростання — локальна спільнота Sunnmøre / Møre og Romsdal.

**Принципи:**
- *Ядро (day-1):* offline-first (архітектурна вимога), локалізація (bokmål + nynorsk + англійська), privacy-by-design (MVP device-local — сирий GPS не залишає пристрій, D14; на сервер — лише v2, агрегований стан «розкрито», не сирі треки), battery management.
- *Gate (тільки природний шар):* безпека — varsom.no + yr.no + disclaimer + кнопка екстреного виклику. Природний шар НЕ виходить у продакшн без цього. Не блокує міський MVP.
- *Дух:* спокій за замовчуванням, челенджі опційні, без streak-шантажу. Доросла аудиторія.
- *Accessibility:* uu-tilsynet / WCAG 2.1 AA — поступово, юридична вимога в Норвегії.

**Соціальне.** М'які друзі + локальні лідерборди, opt-in, **pseudonymous** (нікнейм за замовчуванням, реальне ім'я ніколи не обов'язкове). Без real-time location sharing. **Реалізація — post-MVP (v2)** (причина — обсяг роботи для solo-dev, не GDPR).

**Монетизація.** НЕ комерційний продаж. Норвезька модель суспільно-корисного проєкту: **sponsorship** (sparebank-stiftelser напр. Sparebanken Møre, fylkeskommune, friluftsråd) + **donations**. Рішення свідомо відкладене, без тиску на MVP.

**Успіх.** Локальна спільнота в сотні активних користувачів у Sunnmøre / Møre og Romsdal. Особистий dogfooding.

---

## Технічний стек (планований — деталі в `05`)

**Android:** Kotlin + Jetpack Compose · MapLibre GL Native · Room (SQLite) · WorkManager + Foreground Service · Hilt · Retrofit + Kotlinx Serialization · Health Connect API.

**Backend (лише v2 — НЕ в MVP):** Laravel 11 + Filament 3 · PostgreSQL + PostGIS · Sanctum · Redis · FCM. *MVP — без backend; tiles генеруються CLI-пайплайном (`DECISIONS.md` D12).*

**Дані:** Kartverket — будинки з відкритих **INSPIRE Buildings Core2d + Matrikkelen-Bygningspunkt** (CC-BY 4.0; геометрія Core2d + тип Matrikkelen, експорт напряму з Geonorge — `building2osm` WFS зламаний, `DECISIONS.md` D8); висоти з høydedata.no. *NB: класичний FKB-Bygning — Norge digitalt-обмежений (платний доступ), не використовуємо.* + OpenStreetMap Norway (ODbL) — тест-джерело on-demand. **Доставка будинків — on-demand завантаження зон навколо GPS (D24)**, не фіксований бандл: тест = Runtime Overpass (ODbL), ⚠️ продакшн = pre-hosted CC-BY (INSPIRE+Matrikkelen) на CDN. *(Tippecanoe→PMTiles лишається для seed перф-режимів.)* Базова карта: Kartverket WMTS (`topograatone`). Стежки/вершини/хижки: Kartverket (turrutebasen, Stadnamn, topodata) + DNT. **POI (Nature-v1, D34):** OSM (куратний allowlist) + **Wikidata P18 → Wikimedia Commons** для ілюстративних фото місць (CC-BY-SA; ⚠️ Commons вимагає описовий User-Agent інакше 403; per-image кредит автор+ліцензія — pre-release TODO). *(Деталі → `05`/`06`.)*

---

## Структура документів (`docs/`)

| Файл | Що | Статус |
|---|---|---|
| `01-product-vision.md` | Бачення, позиціонування, принципи, успіх | ✅ Готовий (v0.4) |
| `02-user-personas.md` | Первинна (Денис) + 3 вторинні персони | ✅ Готовий (v0.2) |
| `03-feature-spec.md` | Каталог фіч за шарами, теги пріоритету | ✅ Готовий (v0.1) |
| `04-gamification.md` | Спокійний кор (метрики Coverage/Variety/Discovery, колекції, бейджі — без score) **+ опційний шар §11 (post-MVP, D33)** | ✅ Чернетка (v0.3) |
| `05-tech-architecture.md` | Архітектура, ADR (Kotlin, MapLibre, рендер D22, **edge-матчинг D25**, **on-demand D24**; PostGIS — v2) | ✅ Чернетка (v0.2) |
| `06-data-sources.md` | Kartverket, OSM, varsom, yr — джерела й ліцензії | ✅ Чернетка (v0.1) |
| `07-privacy-gdpr.md` | Datatilsynet, мінімізація даних, DPIA | ✅ Чернетка (v0.1) |
| `08-design-system.md` | Кольори за типами, типографіка, **дизайн-референси §14, мінімалізм/іконки D28** | ✅ Чернетка (v0.2) |
| `09-mvp-scope.md` | Лок MVP-шару (місто-first), що в/поза MVP, acceptance criteria, **анти-патерни жанру §11** | ✅ Чернетка (v0.2) |
| `10-roadmap.md` | Етапи розробки, фази (рознесені D26–D30) | ✅ Чернетка (v0.2) |
| `DECISIONS.md` | Канонічний журнал рішень accepted/proposed/superseded (**D1–D38, P-серія до P31**) | ✅ Створено |
| `CHANGELOG.md` | Хронологія змін (що+чому) + мапа компонентів as-built — для стороннього review (codex) і згадування | ✅ Створено |
| `11-spike-plan.md` | План технічних spike (рендер, трекінг) | ✅ v0.1 |

> **Операційний стан spike** (поза `docs/`): `spike/android-render/SPIKE-STATUS.md` (поточний стан+команди), `SPIKE2-WALKTEST.md` (Stage A/B), `SPIKE2-BATTERY.md` (Stage C), `spike/fieldtest/analyze.py` (аналіз матчингу по польових логах).

---

## Відкриті точки / відкладені рішення

- **MVP-шар** — ✅ вирішено: **місто-first** (`DECISIONS.md` D3 / `09` §1).
- **Фінальний бренд + домен** — чекаємо власника `streif.no`; фолбеки готові.
- **Базова карта** — вирішено в `05`: Kartverket WMTS `topograatone` (raster) + власні pmtiles для будинків; vector-база Kartverket (`landtopo`) поки `/test/`, на потім → `06`.
- **Мова інтерфейсу за замовчуванням** (за пристроєм чи норвезька) → фаза дизайну.
- **Двигун новизни / утримання в малому місті** (P10) — прототипувати й МІРЯТИ retention, не приймати аксіомою.
- **М'яка гейміфікація** (P13) — повнота колекцій числом; ідея щоденної нагороди (без примусу/штрафів). Денис обмірковує.
- **Авто-маршрут до недослідженого** (P16) — час/відстань + кільце/в один бік; v2+.
- **Опційний шар гейміфікації** (D33, post-MVP) — діорама/штамп-альбом (P22; 2D-фідельність, **3D — цільова**), типи-як-точки-дій (P23), баланс без формули (P21), новизна маршруту (P24, лінк P6), історичний шар (P25), **свіжість-retention (P19)**, **умовні/сезонні варіанти місця (P27)**, **географічна completion — kommune/tettsted/острів + емблема (P28)**, **turpostkasse (P29, v2 — кандидат на заміну лідербордам)**; **відкладено:** герої, rarity-класи S–D; **відхилено:** бізнес-штамп-партнерства, «рідкісне в глушині». Coverage-% = **per tettsted + видимий кордон (P20)**. → `04` §11.
- **Екран статистики / прогресу** (P30) — зрізи «розкрито» по **географії × типу** (tettsted → комуна → [острів]); **дані вже є** (P18/P20) → презентаційна задача, **fast-follow/MVP-1**. Гарди: прогресивне розкриття (`08` §1), м'який індикатор, чесний знаменник (D6), без score (D13). → `04` §3.
- **Прогортана мапа не показує вже розкрите (P32)** — ✅ **ВИРІШЕНО 2026-07-31, варіант «а»: камера читає ТІЛЬКИ локальний кеш.** Вибір перевернув факт із коду: `AreaCache` тримає тайли на диску назавжди, а `ensureArea` читає кеш першим, тож скрізь, де вже гуляв, геометрія локальна — мережа не потрібна й **не допомогла б** (шар фарбує лише те, що є в Room). Декларація змін не потребує (`play-data-safety` §2.2a). ⚠️ Там же виправлено власну передумову: «координати того, на що дивишся» покидають пристрій **уже зараз** — підложка Kartverket є raster `{z}/{y}/{x}` для вікна перегляду, не для GPS. **Не лікує порожній кеш** (новий телефон після відновлення, зміна `DataVersion`) → це варіант «в». Альтернативу «малювати з Room точками» **закреслено** — у Room немає координат за задумом (D14). → `DECISIONS.md` P32.
- **Передзавантаження області наперед (P36)** — опція «завантажити комуну/фюльке» в Settings. Сьогодні геометрія приходить **на льоту навколо GPS** (D24), при встановленні не качається нічого, тож «поїхав у гори без мережі» = «побачиш лише те, де вже був». Механіка майже готова (зони вже адресуються координатами й кешуються назавжди); обсяг визначають ⚠️ відсутність полігонів комун · ⚠️ `DataVersion`, що стирає кеш · розмір і кнопка «видалити». ✅ Приватність у **плюс**: одна пачка наперед замість сотень запитів по ходу. Реалізація — не зараз.
- **Дізнатись, що тут є що фарбувати, і куди йти (P39)** — розширює **P33** на весь продукт. Ключове розрізнення: у стежок і POI «що» вже видно (пунктир/іконка), бракує «куди»; у будівель невидимо і те, і те. Пропозиція Дениса — тимчасова підсвітка + масштабування за тапом зі статистики; ⚠️ це варіант (б) з P33 із тією самою ціною (мапа стає квест-трекером, `09` §11). Спершу обговорення.
- **Національний масштаб (P38)** — плитковий шар будівель готовий (не знає про регіон), а **чотири файли тягнуться цілком** і лягають у пам'ять: `manifest` · `tettsteder` (полігони!) · `poi` · `trails`. Поміряно: `trails.geojson` на 2 комуни = 348 КБ → по країні ~**60 МБ**. Стіна виникне **одразу** при виході за Møre og Romsdal. Лікування — зробити їх адресовними за областю; ⚠️ **це та сама робота, що P36**.
- **~16 МБ спайкових залишків у релізі (P37)** — `assets/buildings.geojson` та ін. читає лише perf-шлях; при `.aab` 36 МБ це майже половина. ⛔ Без рішення Дениса не чіпати.
- **In-app Settings-екран (P26)** — самостійний тюнінг без ребілду: **✅ (a) стиль карти Kartverket + вхід ЗБУДОВАНО** (gear-FAB, 4 стилі, swap на льоту + персист, device-verified 2026-07-12); далі **(b)** кольори типів будівель [accessibility], **(c)** акцент/фон кнопок. → `DECISIONS.md` P26.

---

## Робочий процес із кодом (коли дійде до розробки)

- Проєкт **під git від 2026-06-27** (гілка `main`); `.gitignore` виключає APK і регенеровані geo-дані. Працювати в гілці; checkpoint-коміти перед великими задачами.
- Технічний spike першочергово: перевірити, що зафарбовані building-tiles рендеряться в MapLibre на реальному Android-пристрої — це найбільший технічний ризик, його валідувати ДО розробки UI.
- Послідовність (факт станом на 2026-06-27): **Spike-1 v1+v2** ✅ → **Spike-2 Stage A** (FGS+tracking-ядро) ✅ → **Stage B** (vehicle-gate) ✅ → **Stage C** (battery) 🛠️ → польовий ретест + тюнінг → **MVP-0** (Room, eligibility-фільтр D6, CC-BY-дані+CDN-міграція D24, onboarding, локалізація). Збірка/тест — лише через CLI (`gradlew` + `adb`, JBR `JAVA_HOME`), не Android Studio Run. Деталі → `10` / `DECISIONS.md` / `CHANGELOG.md`.
