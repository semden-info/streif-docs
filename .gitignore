# === Build outputs / binaries (регенеруються) ===
*.apk
*.aab
apks/

# === Згенеровані / завантажені geo-дані (регенеруються spike-скриптами) ===
*.pmtiles
*.osm
*.geojson
seed_areas/
_cache_*

# === Python (spike/pipeline) ===
__pycache__/
*.pyc

# === Android / Gradle (коли з'явиться справжній проєкт) ===
build/
.gradle/
local.properties
*.iml
.cxx/
captures/
.externalNativeBuild/

# === IDE ===
.idea/
.vscode/

# === OS / OneDrive ===
Thumbs.db
desktop.ini
.DS_Store

# NB свідомо ЛИШАЄМО у версіюванні: код (.kt/.py/.ps1/.html), доки (.md),
# польові дані (diag_field*.csv, fieldtest/*.csv) і скриншоти-докази (*.png).
# Якщо схочеш лінший репозиторій — додай сюди *.png.

# Локальний кеш wrangler (з'являється при деплої воркера телеметрії з цієї теки).
# Містить ID акаунта Cloudflare і назву акаунта — не секрет, але в репозиторії йому не місце.
.wrangler/

# ⚠️ Відео (записи екрана для FGS-декларації Play). Репозиторій ПУБЛІЧНИЙ, а такий запис — це
# 3 хвилини з телефона власника: екран блокування, шторка сповіщень, швидкі налаштування, реальний
# маршрут вулицями. У першому ж дублі 2026-07-29 у кадр потрапили номер телефона з екрана блокування
# і останні цифри платіжної картки з плитки «Гаманець». Місце таким файлам — поза git.
*.mp4
*.mov
*.mkv
*.webm
